# IsleMind vNext Migration Status

**Updated:** 2026-08-07

**Compatibility cutover:** Complete for the removed storage/product-mode paths. Old writers and the removed persisted formats are no longer supported. `src/services/storage.ts`, `CONVERSATIONS`, `ACTIVE_CONVERSATION_BY_MODE`, persisted `productMode`, `modeConversations.ts`, Agent AssistantRun rewriting/decoding, Tavern v1 envelopes, key-value-to-SQLite migration/state, old multi-key storage, and the portable AsyncStorage recovery-blob fallback are deleted and restoration-gated. Current SQLite conversations plus `ACTIVE_CONVERSATION`, Chat-only AssistantRun rows, current native SQLite Tavern storage, current browser v2 key-value Tavern storage, Web Locks, and durable Chat task/workflow/recovery infrastructure remain supported.

**Durable workspace evidence rule:** `AssistantRun` schema v4 persists the exact captured handoff atomically with `run.created` as strictly validated durable evidence only; it does not grant recovery authority. Unsupported or incomplete no-handoff data is classified as terminal decode-only no-replay inputs at the fail-closed boundary. Recovery never infers effect authority unless an awaited durable final-output/success barrier exists.

**2026-07-30 live-evidence checkpoint:** Current native portable round trips and current browser restart/two-tab Web Locks evidence pass. Those results validate the retained current native SQLite and browser v2 key-value paths; they are no longer deletion blockers for old-writer compatibility because the old writers and formats were intentionally retired.

**Native import boundary:** `src/services/fileImportGuards.ts` is deleted and restoration-gated. Knowledge import, attachment/speech services, and Settings, Skills, and Provider presentation now consume the reusable `@/platform/native/boundedImportFile` boundary directly.

**Overall:** In progress for the wider vNext refactor, complete for this legacy-removal slice. The product shell and runtime are Chat-only. Conversations persist current SQLite rows and one `ACTIVE_CONVERSATION` selection. Assistant Runtime admits only Chat rows; non-Chat rows fail closed and migration v5 is inert. Workspaces retains current scoped snapshots, review/writeback receipts, native SQLite, browser v2 key-value persistence, Web Locks, and portable import/restore, without legacy envelope or cross-store migration readers.

The Conversations-owned `createConversationChatWorkflowReplyStarter` is composed directly in `conversationReplyStart`; explicit workflow dispatch and confirmation restart share the same Chat-only starter. Task activity is `chat-turn | chat-workflow`, catalog discovery and restart are mode-free, and no previous-message product mode enters presentation or runtime.

The Tasks-owned workflow runtime is Chat-only across entry decisions, assistant-message resolution, cancellation, task policy, and confirmation. Catalog discovery and restart are mode-free, and no typed Agent/Companion product-mode input is accepted. Untyped extra properties from an untrusted JavaScript caller are discarded before policy or execution and do not form a compatibility contract. The frozen workflow compatibility evaluator remains test-private and does not expose runtime compatibility.

The current Workspaces runtime policy is Chat-only through frozen `CHAT_WORKSPACE_RUNTIME_POLICY` and parameterless `getChatWorkspaceRuntimePolicy()`. The product presentation uses one `CHAT_PRESENTATION_CATALOG`; no product-mode decoder, `modeConversations.ts`, historical Agent/Companion label/search projection, or alternate workspace runtime selector remains.

## Runnable and transitional paths

- **Chat presentation ownership:** the current catalog, media-readiness policy, and boundary projection live under `src/presentation/features/chat/`; shared mobile layout metrics live under `src/presentation/layout/`. The former `src/product/` tree, `ProductMode` layout flag/resolver names, `productModes` locale namespace, unused mode-tab copy, and historical Agent/Companion labels are deleted and restoration-gated.
- **Chat:** presentation command -> bootstrap-composed Assistant Runtime reply start -> persisted conversation -> durable `AssistantRun` -> provider gateway -> run journal/recovery -> presentation projection.
- **Chat run recovery:** `AssistantRun.kind` is literal `chat`, `AssistantRuntime.recoverInterruptedRuns()` is parameterless, and persistence strictly decodes current Chat rows. Existing non-Chat rows fail closed before projection or recovery. SQLite migration v5 is an inert migration-ledger tombstone; no rewrite or late Agent decoder remains.
- **Assistant Runtime test ownership:** the frozen v1 context-engineering compatibility evaluator is private at `src/modules/assistant-runtime/testing/contextEngineeringCompatibilityEvaluation.ts` and consumed only by its dedicated script. It is not exported from `@/modules/assistant-runtime`; the former `src/services/contextEngineeringCompatibilityEvaluation.ts` path is deleted and restoration-gated.
- **Knowledge:** validated import, scoped retrieval, RAG orchestration/evaluation/replay, memory extraction, portable snapshots, and SQLite hybrid/ColBERT/RAPTOR/GraphRAG indexes are target-owned and bootstrap-composed.
- **Knowledge test ownership:** the frozen v1 memory-governance evaluator is private at `src/modules/knowledge/testing/memoryGovernanceEvaluation.ts` and consumed only by its dedicated script. It is not exported from `@/modules/knowledge`; the former `src/services/memoryGovernanceEvaluation.ts` path is deleted and restoration-gated.
- **Providers test ownership:** the frozen v1 structured-output compatibility evaluator is private at `src/modules/providers/testing/structuredOutputCompatibilityEvaluation.ts` and consumed only by its dedicated script. It is not exported from `@/modules/providers`; the former `src/services/structuredOutputCompatibilityEvaluation.ts` path is deleted and restoration-gated.
- **Providers:** activation, discovery, credentials, capability and ordinary conversation/native-search admission, routing, request/response shaping, streaming, fallback, health, session leases, request hardening, remote compaction, and model/media adapters are target-owned or explicitly bootstrap-composed. Core serializes and reread-verifies secure mutations; Platform exposes the raw Expo SecureStore/namespaced web adapter; Providers preserves existing provider/group key names, rejects sanitized-key collisions, and owns typed rollback-capable credential batches; bootstrap composes the concrete boundary and search/observability cleanup. Settings and portable import use those target APIs. The former `src/services/secureStorage.ts` and `src/services/ai/secureKey.ts` shims are deleted after failure-injection coverage proves read/write/delete/verification errors, successful rollback, rollback failure, stale-key deletion failure, stable redacted import results, and prior-secret restoration. Credential-batch recovery remains independently covered. Whole-application import now includes secure state as its fourth ordered participant and restores verified pre-import credential state during reverse rollback.
- **Rich provider gateway:** bootstrap now creates one `conversationProviderGateway`, binds the concrete `streamProviderChat` adapter only there, and supplies the same gateway to durable `AssistantRuntime`. Initial rich Chat streaming, provider-native tool continuation, and tagged-MCP continuation call `ProviderGateway.startRuntimeStream`; no one of those lanes imports the concrete provider stream runtime. The gateway preserves exact rich request, callback, terminal-result, stream-handle, cancellation, fallback/health, compaction, citation/trace, replay, usage, and tool-call identities, and fails closed without a configured runtime adapter. The callback entry remains a compatibility layer until the canonical `ChatRequest` / `StreamEvent` protocol has complete rich request and terminal-receipt parity.
- **Integrations:** MCP admission/dispatch plus cancellable catalog persistence/discovery, external observations, task lifecycle, registered catalog, Android control-plane policy, toolchain manifests, validated CLI catalog, and the versioned external-agent runtime descriptor/session policy are target-owned or explicitly bootstrap-composed. The external-agent boundary maps `codex-cli`, `codex-desktop`, `claude-code`, and `grok-cli` onto three closed vendor protocols with `stdio` and onto four exact stable task capabilities under `islemind.external-agent.*`. Admission requires the selected product's exact unexpired durable task/runtime identity, caller-supplied resume correlation, and exact `AbortSignal` forwarding; an unrelated `task.run` record or another product's task fails closed before port work, and Android app and Termux runtimes fail closed. It has no concrete adapter, bootstrap binding, persistence repository, process launch, permission/sandbox binding, mobile companion transport, or production execution path, and does not prove resume persistence or signal ownership.
- **Integrations test ownership:** the frozen v1 tool-calling compatibility evaluator is private at `src/modules/integrations/testing/toolCallingCompatibilityEvaluation.ts` and consumed only by its dedicated script. It is not exported from `@/modules/integrations`; the former `src/services/toolCallingCompatibilityEvaluation.ts` path is deleted and restoration-gated.
- **Current Chat workspace path:** Workspaces supplies the current mode-free source, exact-run handoff, receipt-backed writeback/review, portable import/restore, native SQLite repository, and browser v2 key-value repository under cross-context Web Locks. Corrupt, dangling, malformed, cancelled, uncertain, or authority-drifted state fails closed. Tavern v1 envelopes, legacy storage selectors, old multi-key state, and key-value-to-SQLite migration are deleted and restoration-gated.
- **Current Workspaces storage:** Native uses the shared SQLite repository. Browser uses the strict v2 key-value envelope and requires Web Locks for review/writeback. These current stores are selected directly and are not migration sources for each other. Key-value-to-SQLite migration factories/ports/state/markers and old multi-key readers are absent from the Workspaces public API and bootstrap.
- **Portable Tavern workspace migration contract:** `createTavernPortableWorkspaceImportRuntime` is Workspaces-owned and public. It creates and exactly rereads one immutable named backup containing the complete source repository/revision and normalized target identities, commits import or restore only against backup-derived revisions without retry/rebase, verifies a complete fresh repository reload, reconciles same-operation retry and uncertain commits, preserves verified success after post-commit cancellation, and refuses restore after newer drift. Bootstrap binds the current repository, a key-locked AsyncStorage compare-create store, and Expo Crypto SHA-256; the whole-application recovery coordinator consumes only the named import/restore facades as its first participant. Focused concrete SQLite close/reopen and key-value fixtures cover immutable/no-overwrite evidence, corrupt/mismatched/oversized backup rejection, source drift, exact reload, cancellation stages, import/retry, uncertain commit reconciliation, restore/repeated restore, and restore drift refusal. Architecture mutations protect the public export, bootstrap composition, and removal of the raw retrying storage call. The workspace runtime remains independently restorable inside the durable reverse-rollback saga. The coordinator intentionally does not claim one cross-store transaction, and large-backup/live-device behavior is not claimed.
- **Whole-application portable import recovery:** Core owns the recovery envelope/transition rules, Platform owns bounded envelope persistence and the native SQLite before-image blob adapter, and bootstrap composes the fixed participant order. The removed AsyncStorage recovery-blob fallback is not read or cleaned up. Conversations use current SQLite data only; no `CONVERSATIONS` cache is captured or restored.
- **Chat workspace review:** `createChatWorkspaceReviewRuntime`, the native SQLite compare-and-swap port, and the coherent single-envelope key-value compare-and-swap port are Workspaces-owned and public. Bootstrap selects SQLite on React Native, admits the existing key-value envelope only with cross-context Web Locks, and otherwise exposes no review runtime; Chat opens the bounded review sheet from the active-conversation toolbox only when that runtime is available. Approve, dismiss, and private-memory purge re-read current state, require exact authority, preserve runtime-issued confirmation tokens, abort superseded operations, and expose no private content or raw failure reasons. Focused key-value coverage proves restart across repository instances, exact-key serialization, same-realm cross-instance CAS, stale-link and corruption rejection, invalid-snapshot admission, private confirmation, cancellation stages, exact post-write verification, committed-success cancellation semantics, and post-write drift detection. The current Edge persistent-profile/two-tab run supplies browser persistence/restart and cross-context evidence for this key-value parity, so the durable web key-value capability is promoted.

- **Conversation store persistence boundary:** `ConversationStorePersistencePort` is bound to the current SQLite repository and single `ACTIVE_CONVERSATION` record before hydration. The Chat store has no legacy storage import, `CONVERSATIONS` fallback, background compatibility hydration, or `ACTIVE_CONVERSATION_BY_MODE` path.
- **Settings and provider-metadata persistence boundary:** Settings and Providers expose owner-defined record ports, and bootstrap binds the current `SETTINGS`/`PROVIDERS` application-record adapters before hydration. `src/services/storage.ts` is deleted and restoration-gated.

## Active compatibility layers

- `@/core` exclusively exports `ReasoningEffort` and the generation-parameter key/source contracts. All 15 remaining production consumers now import `ReasoningEffort` from `@/core`; the former `providerContracts` and broad `types` barrel re-exports are deleted and the architecture audit blocks their restoration. `providerContracts.ts` retains only a private Core import for `AIModel.reasoningEfforts`, so this cutover changes no runtime or persisted schema.
- Secure credential storage is target-owned by Core, Platform, Providers, and bootstrap. Data Management owns current portable export/import/reset and recovery-plan construction; bootstrap binds current application records, SQLite conversations, Knowledge, Workspaces, and secure state. `src/services/storage.ts`, `src/services/portableData.ts`, `src/services/skills.ts`, `src/services/localDataStore.ts`, `src/services/secureStorage.ts`, and `src/services/ai/secureKey.ts` are deleted and restoration-gated. No fixture/source alias or older-writer condition remains.
- `ConversationSnapshot` v2 persists reasoning effort and generation-parameter overrides. The parser continues to admit v1 and unversioned Conversation data; a legacy record without an override map treats its finite stored generation values as explicit so historical request behavior is preserved. This branch may be deleted only after persisted data has an explicit migration version and v2 migration fixtures cover the cutover. Override maps are materialized lazily on the next save rather than by a bulk database rewrite.
- Generation-parameter source maps are required on Core Chat, Provider Runtime, and provider-planning requests. Every production request constructor supplies explicit provenance; untyped input without a map fails closed as provider defaults and numeric values are omitted rather than inferred as `explicit`. Focused behavior and architecture mutation coverage block restoration of the deleted inference path.
- `ProviderGateway.startRuntimeStream` is the Providers-owned compatibility bridge for rich Chat while `ChatRequest` / `StreamEvent` remain deliberately smaller. It may be deleted only after attachments, retrieval and web-search context, fallback state, remote-compaction admission/evidence, continuation state, provider-tool declarations, traces, citations, usage, replay content, response identity, and a validated terminal receipt are represented by the canonical protocol and focused initial/continuation/finalization tests pass without the callback bridge.
- Settings accepts legacy family inputs `cartoon | island -> lime-road` and `glass -> markdown` so persisted data, agent/tool schemas, and legacy CSS selectors continue to resolve safely. The canonical runtime family union contains only `minimal`, `lime-road`, and `markdown`; these aliases may be deleted only after a persisted-settings migration audit and consumer audit prove no remaining legacy input.
- `useAppTheme().isGlass`, material/property aliases, the legacy `colors` export, CSS alias selectors, and locale compatibility keys remain dormant compatibility surfaces for existing consumers. None is currently deletion-safe; every removal requires a consumer-free audit plus focused Chat/settings coverage and persisted historical-data migration fixtures.
- Theme experience maintenance now has a pure theme-keyed extension registry plus a central `animal-island-ui` review contract. Minimal, Lime Road, and Markdown all retain Isle UI interaction and accessibility compatibility; Lime Road records the fused `animal-island + summer-road` composition and `road-cinema` motion profile. The upstream review is npm `1.5.1` at `803cffa` (2026-08-07), including the native `IsleTime` `hud | game` adaptation. Semantic page/scenic motion has opacity-only reduced behavior and immediate none behavior; expansion into Chat messages, floating controls, or scroll-bound parallax remains gated by focused performance, accessibility, and cancellation tests.
- Integrations owns both the immutable application built-in catalog (`app_info` plus six Settings actions), the bounded four-capability catalog, and the configuration-neutral external web-search provider adapter. Bootstrap composes canonical app-info projection, the target Settings command, configured search credentials/endpoints, and injected fetch without a service registry. Cancellation-checked `app_info` succeeds without a task because it returns immutable application/transport metadata; `get_settings` and all five Settings mutations require exact running/allowed durable task admission before any Settings access, under canonical `builtin:islemind-builtins:<action>` model-visible identities. Duplicate Settings `app-action:*` identities are removed from model-visible catalogs; explicit Settings-screen controls continue through the structured Settings command. Focused app-info/settings/search success, failure, malformed-response, unsafe-endpoint, cancellation, catalog-mutation, MCP, task-admission, and architecture restoration coverage allowed `src/services/builtinToolRegistry.ts` and `src/services/searchAdapters.ts` to be deleted. `search_web` remains production-bound through exact durable task reread admission and the configured search-provider port. New settings select zero-credential `islemind` by default; this fixed Bing public-RSS adapter is best-effort and does not claim an IsleMind-hosted index or availability SLA. Its structured XML parser disables general entity expansion, decodes only fixed XML named entities, bounds response text and records, propagates cancellation, and admits only deduplicated public-HTTPS results. Fetch/crawl remain separate known-URL retrieval capabilities rather than alternate search implementations. Tavily requests omit the optional generated answer, disable raw page content, use basic depth, and respect the documented 20-result maximum; target projection validates and deduplicates canonical public HTTPS URLs, preserves the first ranking, and continues scanning for distinct bounded results. Google Custom Search remains only for existing customers through its January 1, 2027 discontinuation. The `bing` setting denotes an explicitly configured compatible endpoint because first-party Bing Search APIs retired on August 11, 2025. Bing-compatible and custom endpoints require public HTTPS on the standard TLS port, but hostname admission does not claim DNS resolution-to-connection pinning. `crawl_web` is production-bound through a local-first route. Android prefers the native DNS-to-connection-bound trust/manual-fetch pair whenever both ports exist; every redirect is separately readmitted, and a local trust or Fetch failure is returned explicitly rather than falling through to a vendor. Tavily remains an explicitly remote-derived fallback only when the local pair is absent: depth zero maps to single-URL extraction, positive depth maps to bounded same-origin crawl, and target validation rejects malformed, private, cross-origin, duplicate, over-depth, over-page, over-byte, and over-text vendor output. The remote port does not claim local DNS-to-connection pinning. `read_file` is production-bound through `createKnowledgeWorkspaceFileReadPort`: only ready durable Knowledge imports can be read from independently contained `knowledge/<documentId>.txt` virtual paths; ordered chunks, content-hash revision, `text/plain`, byte bounds, and cancellation are checked before projection, while picker caches, source URIs, and database paths stay inaccessible. Native bootstrap additionally binds an independently contained SQLite `workspace/*` namespace. `edit_file` is advertised there only after exact durable task admission; create uses `absent:v1`, updates use SHA-256 CAS revisions, and file state plus idempotency receipt commit atomically with UTF-8, file-count, total-byte, cancellation, replay, and conflict checks. The web binding omits `edit_file` because Expo SQLite web support remains alpha and the writable capability lacks a real-browser restart proof; the shared SQLite adapter itself is persistent. The unrealized built-in media tool manifests, ports, policies, adapter branches, and tests are deleted and restoration-gated. Future media-artifact tooling requires a new durable contained design and focused proof rather than restoring the removed contracts.
- Integrations also owns immutable web-search option ordering with `islemind` first, effective provider resolution, legacy `native | tavily | off` synchronization, the diagnostic query, and HTTP(S) endpoint-template validation through its public API. Persisted explicit search-provider selections remain valid; the new default does not remove or rename any prior provider ID. Store, readiness, workflow admission, Assistant Runtime context acquisition, provider configuration, Tavily crawl, and context self-test consumers use that target policy; Settings presentation separately owns localized provider labels and credential-field metadata. Focused behavior and restoration coverage allowed `src/services/searchPolicy.ts` to be deleted.
- Integrations now owns a contract-only external-agent runtime boundary through `externalAgentRuntimeContracts.ts` and `externalAgentRuntimePolicy.ts`. Exact v1 descriptors admit only the closed `codex-app-server`, `claude-code-stream-json`, and `grok-acp` protocol mappings over `stdio`; the frozen public task-capability map binds `codex-cli`, `codex-desktop`, `claude-code`, and `grok-cli` respectively to `islemind.external-agent.codex-cli`, `islemind.external-agent.codex-desktop`, `islemind.external-agent.claude-code`, and `islemind.external-agent.grok-cli`. Requests require a trusted online desktop or remote runtime, `cli` plus `task.run`, a matching unexpired running durable task with `task.run` permission and the selected product's exact stable task capability, and an exact `descriptorId + runtimeId + taskId` authority tuple. Unrelated trusted tasks and cross-product tasks fail with `task_tool_mismatch`; execution rechecks the same binding before port dispatch. Android app and Termux runtimes fail closed, and task expiry is rechecked before both request creation and port dispatch. Request and outcome v1 wire shapes are unchanged. Matching request admission now passes for all four products, and descriptor/request admission is covered against mutation or freezing of caller-owned input; only detached normalized results are frozen. Resume requires explicit descriptor support and a caller-supplied protocol-specific vendor-session correlation identity; no persistence ownership is proven. The policy forwards only the caller's exact `AbortSignal`, performs no port call when already aborted, suppresses outcome admission after cancellation, and admits only exact-key bounded immutable outcomes matching the complete request identity; this module does not prove signal ownership. Command/path/argv/environment/credential/header/endpoint/`User-Agent` configuration is absent and unknown metadata fails closed. This contract does not emulate a vendor, prove a binary or protocol, or restore the deleted CLI execution, runtime-report, pairing/handoff, or `toolchain.*` event stacks. No compatibility layer is deletion-safe from this slice. A future bootstrap adapter must provide real vendor protocol evidence, authoritative task reread, trusted persisted-session lookup, task-signal ownership, permission and sandbox enforcement, cancellation, idempotency, restart/no-replay, redaction, and desktop/device proof before any product/runtime can be advertised as executable.
- Durable task identity now flows from the persisted Agent task through the MCP execution policy and bootstrap call seam for both remote and built-in dispatch. Callers without a durable task retain the synthesized remote ID only as a compatibility fallback; no legacy registry or target adapter is removed by this slice.
- Remote context compression is now route-evidence-bound on admission, reusable state, active-state persistence, outcome recording, and provider fallback. Assistant Runtime resolves the exact provider, upstream model, admitted web-search mode, and sendable attachment array before planning; bootstrap resolves the Providers context-management decision, and Providers admits native OpenAI compaction only for explicit `true` Responses evidence. The exact `native-openai-responses`, `native-compaction`, and `remote-available` decision now crosses reply start, stream lifecycle, and finalization unchanged. Reusable continuation state requires that triple before repository lookup, active state persistence requires it on both lifecycle input and sanitized usage, and completed/failed outcome inputs require all three decision fields. Omitted runtime evidence is not upgraded to `remote-available`; explicit `false`, omitted, Anthropic, local, unavailable, or failed evidence fails closed. `auto` retains the privacy-gated structured local fallback and `required` blocks. Direct Anthropic compaction emits `context_management.edits[{ type: "compact_20260112", trigger: { type: "input_tokens", value } }]` with beta header `compact-2026-01-12`. Provider fallback recomputes route eligibility and, for a non-native route, clears Responses continuation/compact fields and sends the prepared bounded local messages/context. The remaining fallback executor is compatibility code, not a deletion candidate.
- New compatibility boundaries must name their owner, target API, focused proof, and deletion condition before they are introduced.

## Current deletion queue

This deletion queue is complete for the requested legacy slice. Agent run rewriting/decoding, product-mode conversation metadata and selectors, raw storage aliases/caches, Tavern v1/old multi-key readers, key-value-to-SQLite migration, portable recovery-blob fallback, the `ProviderProbe` alias, and the duplicated provider-probe `detail` evidence field are removed. Provider probing exposes only `ProviderProbePort` and `redactedReason`. Architecture and contract gates enforce their absence. Remaining compatibility items listed elsewhere belong to separate current contracts and are not deletion blockers for this cutover.

## Current generation-parameter slice

- The Core ownership cutover is complete: `ReasoningEffort` has one export location at `@/core`; Providers and bootstrap consumers import it directly, and architecture self-test coverage rejects restoration of the retired provider-contract/type-barrel re-exports. `ChatRequest`, `ProviderRuntimeChatRequest`, and provider planning now require generation-parameter provenance; every production constructor supplies it, missing untyped provenance fails closed as provider defaults, and architecture mutation coverage rejects restoration of numeric-value-to-explicit inference. This changes request admission for invalid untyped callers but does not change persisted schemas or annotated production request shaping. The remaining deletion candidate is the v1/unversioned Conversation override reconstruction branch.
- The ordinary Chat path now preserves `reasoningEffort` and generation-parameter provenance through persisted Conversation -> `ConversationSnapshot` -> `ChatRequest` -> `ProviderRuntimeChatRequest`. A new Conversation starts with an empty override map, meaning provider-native defaults; any valid user-entered finite value is `explicit` even when it equals a recommended value, and only Restore Default removes that override.
- Provider request preparation creates an endpoint-aware per-field plan before body serialization. A parameter is sendable only when model metadata, provider declaration, reasoning constraints, endpoint serializer capability, and a real wire path all permit it. Provider defaults are omitted; protocol-required values use bounded `internal-policy` fallback with an explicit `required-endpoint-fallback` reason.
- OpenAI Responses Top-K is `unsupported-endpoint` and no unsupported `top_k` field is added. An explicit stored Top-K value survives in the Conversation so switching to a compatible model can restore it. Anthropic and Google endpoints with actual serializer support continue to send their supported fields.
- Conformance combines the pre-serialization plan with the generated body, preserving planned omissions while recording retained, removed, adjusted, or unexpectedly included fields. The Parameter Matrix exposes the same per-field result; Sampling and Token Budget aggregate statuses are derived from those fields rather than from `maxTemperature` or another proxy.
- Chat and Preferences generation inputs use local string drafts. Intermediate `0.` and empty states never write numeric zero into the store; decimal and integer controls use distinct keyboards and parsing, and blur/submit applies the current model range. Chat empty submission restores provider defaults, while Preferences invalid or empty submission restores the last valid value.

## Current homepage, toolbox, and provider compatibility slice

- The Chat empty state renders one current Chat introduction and provider starter. History has no product-mode filter, mode-specific creation, historical label/search decoder, or alternate conversation route. Product-mode presentation files and metadata are deleted and restoration-gated.
- Settings now owns a bounded optional assistant display name and up to 128 normalized model display aliases keyed by exact `providerId + modelId`. Hydration, live writes, portable import, and portable export remove malformed values; Chat presentation resolves valid names in its empty state, composer, generic live-thinking fallback, accessibility/system activity, header, quick model controls, references, and picker while retaining trace/tool stages and canonical IDs for routing, search, diagnostics, and duplicate disambiguation. Reply-session initialization creates the canonical Chat assistant message without a product-mode field; forged historical mode metadata is discarded before message projection or runtime dispatch. The name and aliases remain presentation-only: they do not alter system prompts, Assistant Runtime run identity, provider catalogs, routing IDs, or historical messages.
- Chat message actions no longer expose `Send to Agent`, `Summarize in Chat`, or `Bring into Tavern`. The callback chain, feed target derivation, action adapter, five message locale keys, and handoff text in boundary accessibility copy are removed; product and architecture self-tests reject their restoration. The Workspaces handoff domain remains only for the retained isolated Tavern review workflow and is not reachable as Chat product-mode navigation.
- The lower-right toolbox uses a 48 dp trigger, horizontal 44 dp action rows, semantic theme surfaces, localized labels, explicit expanded state, backdrop dismissal, and a scroll-bounded panel. Function-valued row styles are excluded because React Native Web resolved them as column layout. The short-screen panel reserves 64 px of top clearance, and the evidence collector waits for stable animation-frame bounds before measuring controls.
- The historical Expo Web result contains 18 zero-console-error captures, including the 320x568 toolbox measurements, but its Agent and Tavern product captures predate the Chat-only routing contract. The current 16-capture worklist retains unified History, Chat, Chat toolbox, and the 13-case Appearance matrix; the evidence gate reports the old result as stale until that exact set is recaptured.
- Providers-owned client compatibility now defaults each provider to `auto`, selecting Codex CLI/Desktop, Claude Code/Desktop, Grok Build, or IsleMind from the effective model on initial and fallback requests while identifying every profile transparently as IsleMind. Provider details expose only protocol-compatible bounded profiles plus Automatic and IsleMind; missing or malformed persisted/imported values normalize to `auto`, incompatible forced profiles fail closed to IsleMind, and portable export retains the normalized setting without changing provider model records. Current `gpt-5.6-sol`, `gpt-5.6-terra`, and `gpt-5.6-luna` identifiers select the Codex CLI profile, and `grok-build-0.1` selects Grok Build. The policy adds only the standard `User-Agent`, preserves caller-supplied casing, rejects arbitrary header text, does not emulate proprietary telemetry or undocumented authentication, and remains excluded from direct Bedrock Runtime after SigV4 canonicalization.
- Provider-native search now follows the current direct-vendor contracts. Direct OpenAI Responses emits `web_search`; `web_search_preview` remains an explicit compatible-relay input only. Direct current Anthropic emits `web_search_20260318` and omits optional `response_inclusion` so the documented `full` response remains intact; Anthropic-compatible relays retain `web_search_20260209`, and legacy models retain `web_search_20250305`.
- External-search compatibility was rechecked against the official [Tavily Search API](https://docs.tavily.com/documentation/api-reference/endpoint/search), [Google Custom Search JSON API](https://developers.google.com/custom-search/v1/overview), and [Microsoft Bing Search API retirement notice](https://learn.microsoft.com/en-us/lifecycle/announcements/bing-search-api-retirement). No paid live-vendor request was performed; focused fixtures validate the documented request shapes and lifecycle restrictions.
- Native Gemini generation, discovery, and provider probing use `x-goog-api-key`; credential material no longer enters request URLs. GitHub Models discovery and probing use `GET /catalog/models` with Bearer authentication plus the documented GitHub JSON media type and `2022-11-28` API version. Provider SSE parsing accepts `data:` with or without one space, multiline data fields, comments, CR/CRLF/LF framing, fragmented CRLF boundaries, terminal markers, and the legacy independent-JSON-line fallback.
- Bedrock Responses/tools and Xiaomi Responses remain fail-closed without fixture-backed routing. Model-family client matching is explicitly user-requested inferred behavior, not a vendor compatibility requirement. Current official evidence URLs and claims are retained in the Providers compatibility catalog; no new production capability is enabled solely from documentation discovery.
- Provider identity migration is complete for new writes: supplier identity and wire protocol are separate current contracts, custom relays persist as `presetId: 'custom-endpoint'` with an explicit supported `wireProtocol`, and compatibility evidence uses its own supplier-or-protocol key. The two former protocol-shaped custom preset identities, their normalization adapter, and protocol-preset registry remain absent from the current public contract. The Providers persistence boundary performs a one-time read-and-write migration for those two known historical identities, while unknown preset or protocol values still fail closed before hydration or recovery; portable imports remain strict and do not re-admit retired identities. The vNext architecture boundary gate prevents either retired identity from becoming a current registry or UI option.

## Milestone F hardening and evidence

- The signed-release and runner-debug workflows install from authoritative `bun.lock` with pinned Bun and no npm path. Signed release blocks before Android prebuild on release-readiness, runtime-budget, runtime-privacy, vNext architecture, walking-skeleton restart-recovery, and task-runtime recovery contracts. Representative-device collectors remain outside GitHub-hosted build jobs because those jobs have no attached Android device.
- Historical release evidence records `dist-apk/IsleMind-0.0.13-arm64-v8a-no-model.apk` at SHA-256 `f122c2f41c45d9a13e989ec48b9be20dc1cb33010815c68fdf34988ffaf65c33` and `IsleMind-0.0.13-x86_64-no-model.apk` at SHA-256 `1d4aa6d60aa7391b025ccfe5fa48cae928cd6988a95d4bbef7caf3472a2815aa`, with prior install, launch, ABI, and 16 KB alignment proof. Their recorded source snapshots predate the Chat-only route, native-intent, pager, and History changes, so neither artifact is current-tree release evidence. A rebuild, clean install, current-APK smoke, and refreshed provider/mobile collectors remain required.
- The prior x86_64 run passed all nine Provider Runtime scenarios under the former contract: provider settings, import with keyboard, chat model switch, blocked-model recovery, fallback evidence, provider state, Android Back, shell relaunch recovery, and provider activation. The import-keyboard and activation evidence remains useful, and the strengthened restart contract now proves a durable conversation/run/journal relationship. The collector sends fixed non-secret ordinary-Chat request/completion sentinels, force-stops before a quiescent SQLite snapshot, requires the known conversation plus a linked `chat` `AssistantRun`, `done` assistant message, and matching `run.succeeded` journal sequence, then requires both sentinels after relaunch. Bun and provider focused self-tests reject unrelated conversation identity, non-Chat or nonterminal runs, missing/mismatched messages, and unlinked terminal journals.
- The historical physical-device Provider Runtime result passed all nine scenarios with zero collector errors and zero contract issues. It predates the recorded APK and current Chat-only source, so current device routing and recovery remain unverified; the non-debuggable black-box result also cannot replace full linked conversation/message/AssistantRun/journal proof when private-storage inspection is available.
- The fresh arm64 Context self-test completed with 8 passing steps, 1 needs-configuration warning for disabled web search, and 0 failures. The collector temporarily enabled memory, local knowledge, and hybrid RAG, then restored the exact original `memory=false`, `knowledge=false`, `ragMode=off` state and recorded the proof.
- Context restoration performs a fresh post-settle observation of all three controls and fails closed on delayed persistence drift; focused compatibility coverage exercises both exact restoration and setter-success/final-observation mismatch. The refreshed arm64 raw result and independent `restore-after` UIA read both prove the exact original `false/false/off` state.
- Settings portable data round-trip evidence is now an aggregate QA result gate. `node scripts/collect-settings-state-android.js --portable-roundtrip` seeds a compact synthetic export with one current Chat conversation, one provider, one independent Tavern workspace scope, and `hapticsEnabled=false`, then proves import, export recovery, visible clear-all, and re-import restore using hashes/counts/paths only. Because the flow clears app data, it fails closed unless `QA_DEVICE_SERIAL` is explicit and `QA_ALLOW_DESTRUCTIVE_SETTINGS_ROUNDTRIP=1`. The current physical-arm64 result at `test-evidence/qa/settings-portable-data-roundtrip-results.json` now passes: the rebuilt release APK was installed on `dadaa813`, the destructive collector completed with `errors: []`, imported the seeded fixture, published `islemind-export-2026-07-23T04-28-01-180Z.json` to public Downloads via `android-public-download` at `/sdcard/Download/islemind-export-2026-07-23T04-28-01-180Z.json`, and proved `chatsCleared=true`, `settingsCleared=true`, `tavernCleared=true`, `restoredAfterRoundTrip=true`, and `hapticsRestored=true`. The collector self-test still rejects both the empty Settings provider state and preview/search misselection as completed import evidence.
- That 2026-07-23 device result exercised the superseded portable-data service path and remains historical only. Current-tree replacement evidence now passes on the rebuilt arm64 release APK installed on `dadaa813`: `test-evidence/qa/settings-portable-data-roundtrip-results.json` proves the compact import/export/clear/re-import flow, and `test-evidence/qa/settings-portable-large-backup-roundtrip-results.json` proves a 12,587,104-byte fixture with a 12,582,912-byte system prompt, byte-count parity in both 12,593,866-byte exports, workspace/provider/conversation recovery, `errors: []`, and `restoredAfterRoundTrip: true`.
- The fresh x86_64 long-content run records a streaming `qa-ultra-long-model-name-with-an-intentionally-verbose-display-label-for-layout-stress` request with `max_tokens=4096` and a detached non-stream memory-extraction request with `max_tokens=512`. Its quiescent device SQLite snapshot proves the linked `AssistantRun` and assistant message succeeded, the append-only journal ended in `run.succeeded`, a model-derived `QA_LONG_CONTENT_PREFERENCE = cyan-lake` memory is pending, and the persisted memory trace is `done` with `transitionStatus=completed`, `signalAborted=false`, and `addedCount=2`. Canonical paired captures cover independently long provider and model labels, a long trace, a long citation, and a long knowledge document. The collector and aggregate audit fail closed on missing durable rows, terminal traces, canonical pairs, UIA markers, request-row parity, or stale same-run provenance; superseded manual SQLite, SecureStore, UIA, and trace snapshots were removed.
- Raw-evidence contract v2 binds source and canonical SHA-256, byte size, numeric and ISO modification time, and record count. Knowledge self-test, long-content, local-model download, and corrupt-mirror inputs all pass, so the aggregate raw contract reports 4/4 passing. The normalized long-content JSONL is byte-identical to raw SHA-256 `aa632feaf8c9bb84a11a36dd14e22d81f399d0accb2f503fe39dbbd9210597c8`; the five-record corrupt-mirror JSONL is byte-identical to raw SHA-256 `dd31e849f9e6b1f6c14bad27dce152afd9b85ffd1d8dd91fc92ea2b13c36ac5d`.
- The x86_64 local-model collector proves a fresh-install `all-MiniLM-L6-v2` download through an emulator-only `http://127.0.0.1:<port>` mirror backed by an ADB reverse lease, with the final row enabled and the mirror value surviving app restart. The corrupt scenario records four successful metadata requests followed by the expected `500 corrupt mirror fixture` for `onnx/model_quantized.onnx`; the successful scenario records a `200` ONNX response. External device networking remains disabled throughout this loopback-isolated evidence path.
- The historical arm64 key-visual run covered the composer Tools and model-picker overlays and a shell-free direct Chat route, but it is not current Chat-only product proof. The top-session options and app-shell error/update captures remained fail-closed gaps, and the mobile worklists still include removed Agent/Tavern product surfaces.
- Structured work-artifact actions now have current paired device proof for the imported artifact, the canonical `actions-open` state, copy confirmation, and continuation prompt. The quick-action row reserves a non-shrinking 44 dp height, both visible actions measure 44 by 44 in UIA, and virtualized message layout is invalidated when the default row is present.
- The refreshed x86_64 mock-provider run proves provider import/test, in-flight and completed streaming, long-press message actions, Delete confirmation, source navigation, and Android Back return. The collector derives horizontal action-row swipes from current UIA bounds, while the aggregate audit now requires the passing interaction result, all canonical capture pairs, and exact request-row parity with the JSONL. Horizontally clipped action children are classified as scroll-edge partials only; a focused fixture keeps genuinely small in-viewport controls blocking.
- The checked-in aggregate QA result reports 23/23 passing checks, but the current evidence gates still admit pre-cutover Agent/Tavern worklists and captures. It is historical evidence for its recorded source set, not proof of the current Chat-only UI, APK, routing, or Provider Runtime recovery. The worklists and freshness contract must be updated before new captures can close those gaps.

## Current appearance reconstruction

- Settings owns normalization of the three selectable families, explicit light/dark/system mode, and optional custom hexadecimal accent. Persisted values and live writes are normalized before palette projection; invalid modes retain the safe system fallback, invalid live family writes retain the current valid family, and invalid accents are rejected or cleared only through the explicit reset path.
- Appearance is one compact Settings foldout: family, mode, and accent choices use named radio groups with one checked mode; a custom accent field stacks its apply action below 360px. All family palettes expose the same semantic surface, content, chrome, control, feedback, message, and accessibility tokens. Markdown is deliberately opaque and document-like rather than a faux Liquid Glass surface.
- Web fallback CSS has critical primary/control/icon parity checks against the canonical runtime palettes. Dialog close affordances meet the 44dp target. The aggregate `test:theme-system` gate includes source, architecture, contrast, and QA coverage so custom-color contrast cannot be skipped.
- The historical Expo Web + Playwright Appearance evidence covers Minimalist, Cartoon, and Markdown in light and dark at 320px and 360px, plus Markdown dark with a custom indigo accent at 360px, with zero console errors. Its Appearance and toolbox observations remain useful, but the result contains removed Agent/Tavern and history-filter captures. The current gate therefore classifies the whole result as stale until the exact Chat-only worklist is recaptured.
- Historical physical-device evidence on `dadaa813` covers History, Chat, Agent, and Tavern product surfaces plus Chat and Agent boundary-detail interactions with zero fatal-log findings and zero clipped primary controls. The current native worklist instead requires unified History, Chat, the opened Chat toolbox, and the Chat boundary-detail interaction; `/agent`, `/companion`, and Agent/Companion/Tavern native intents are source-verified redirects into Chat rather than product screenshots. The old result is classified as stale and current native proof remains pending. The separate native Settings collector still records its nine historical Appearance rows.
- Process spinner, typing dots, streaming cursor, source skeleton pulse, and source loading indicator loop only at full motion. Reduced and disabled motion render stable visible states with unchanged dimensions; the theme QA gate rejects restoration of unconditional looping.
- The full serial theme/product/release, provider intelligence, streaming/presentation, Agent/Tavern, walking-skeleton/architecture, dependency-audit, and type-check matrix passes after the final edits. `remoteCompactMode: "off"` again performs bounded local context packing, provider-native compaction still receives the untrimmed probe, and application-model summarization still uses its local fallback.

## Current removals

- Portable native transfer is platform-owned through `createExpoPortableDataTransferPort`, while Data Management composes it with an explicit payload port and projection boundary. Expo picker/file/sharing behavior, Android Downloads publication, bounded content-URI fallback, and unconditional selected-copy cleanup no longer live in a service facade. `src/services/portableData.ts` is deleted; focused Data Management, portable-import, Tavern, type, architecture, dependency, and mutation gates protect the replacement. The remaining `src/services/fileImportGuards.ts` is only a compatibility re-export of `platform/native/boundedImportFile` for adjacent import surfaces and does not participate in portable-data composition.
- Rich Chat dispatch now freezes a durable `ContextSnapshot`, creates and starts a `kind: 'chat'` `AssistantRun`, publishes the provider start only after durable executor entry, and settles success only after finalization completes. Initial provider streaming and both provider-native and tagged-MCP continuation turns enter the same Providers gateway; the concrete runtime is composed once behind it, and the durable runtime receives that exact gateway instance. Cancellation, persistence failure, terminal-before-start, rejected duplicate publication, thrown execution, missing terminal lifecycle callbacks, and unavailable gateway adapters fail closed. Reply start depends on `conversationAssistantDurableExecutionRuntime`; direct `providerDispatchRuntime.dispatch`, direct continuation imports of `streamProviderChat`, and the older provider-dispatch bootstrap binding are restoration-gated.
- Provider model testing now delegates connectivity to a discovery-backed no-generation probe. New-provider onboarding automatically probes the protocol, discovers models, performs one bounded no-generation readiness probe, enables the provider, and selects the ready provider plus its successful `lastTestModel`; presets, protocol overrides, and manual model lists remain available under Advanced. The prior second enable confirmation and default skipped-probe activation are removed. A failed onboarding remains editable and cannot replace a null default merely because storage added it. Providers publicly export the probe and usage/query contracts; Diagnostics publicly exports the SQLite usage repository factory; bootstrap composes both without deep imports and imports legacy assistant-message usage exactly once as `operationSource: 'chat'`. Generating model-test restoration and Agent/Tavern mode-derived legacy usage are architecture-gated.
- The Expo SQLite adapter no longer substitutes a no-op database on web. It opens the installed worker-backed database, keeps native exclusive transactions, selects `withTransactionAsync` on web, bundles the WASM asset, and emits COEP/COOP headers through Metro. Expo SDK 54 documents web support as alpha. Focused mocked persistence/transaction probes plus source/type validation pass, but live browser durability remains blocked: desktop Chromium, Chrome, and CDP attempts timed out, and the bounded Android Chromium fallback reached the physical Chromium 135 `/json/version` endpoint before Playwright timed out connecting to its loopback-forwarded `/devtools/browser` websocket. The collector records that exact blocked result and removes its owned ADB forward/reverse leases.

- Ordinary reply-session initialization is Assistant Runtime-owned through `createAssistantConversationReplySessionRuntime` and bootstrap-composed `conversationAssistantReplySessionRuntime`. It preserves stop-before-read order, one timestamp/ID/controller, exact streaming assistant message, task title/provider/model metadata, streaming projection, active-handle identity, and a typed missing-conversation no-work outcome while fixing every new message and task to Chat. Historical Agent/Tavern caller values remain decode-only compatibility inputs and cannot select reply-session execution. The inline Chat initializer, fallback mode resolver, and direct product-mode/task dependencies are deleted and gated against restoration.
- Ordinary Chat/Tavern provider and model admission is Providers-owned, while Assistant Runtime owns terminal outcome mapping and ready-only compatibility tracing through `createAssistantConversationProviderAdmissionRuntime` and bootstrap-composed `conversationAssistantProviderAdmissionRuntime`. Bootstrap binds model-access admission, legacy credential hydration, configuration validation, model alias/catalog resolution, generation-parameter shaping, localized failure projection, compatibility-trace creation, sanitization, and storage. Typed setup/rejection outcomes and `{ failed, provider_hydration_failed, unknown, exact providerId }` map without exposing raw errors; only ready outcomes create a trace, and only its sanitized form is stored. In-flight cancellation suppresses downstream configuration, alias, catalog, generation, failure, and trace effects. Disabled/manual provider precedence, local-setup precedence, missing credentials, exact configuration error identity, normalized parameters, terminal projection, ready identity/order, and input nonmutation remain covered. The inline Chat mapping/trace block and direct provider-admission, setup-guide, and compatibility-trace dependencies are deleted and gated. `chatRuntimeResolution.ts` plus the private resolution helpers remain deleted and gated against restoration.
- Bounded ordinary plain-Chat handoff is Assistant Runtime-owned through `createAssistantConversationPlainChatHandoffRuntime` and bootstrap-composed `conversationAssistantPlainChatHandoffRuntime`. Eligibility, persisted-conversation lookup, pre-save cancellation, shallow generation-field merge, exact save/start/register identity, fulfilled/rejected matching-stream cleanup, replacement-stream preservation, undefined-start continuation/cancellation, and localized terminal failure projection remain covered. Configured web search is ineligible because its Integrations-owned external-context work is composed by the full reply-start lane; the thin runtime cannot bypass that stage. Chat consumes one typed outcome; ineligible, missing, and not-started outcomes retain the full context/provider path, while started, cancelled, and failed outcomes stop it. The inline handoff block, private cancellation helper, and direct persistence, plain-Chat runtime/controller, `Conversation`, and active-stream lifecycle dependencies are deleted and gated.
- Ordinary Chat/Tavern provider-native search admission is Providers-owned through a pure policy and bootstrap-composed provider/model support policy. The remaining reply starter consumes one typed outcome for transport mode, exact requested-mode metadata, attachment display suppression, and independent provider/model compatibility evidence; the model-picker badge consumes the same bootstrap support decision. Supported native search, attachment suppression, unclaimed providers, model rejection, combined attachment/rejection evidence, all non-native mode identities, evaluation order, and frozen-input nonmutation are covered. The duplicate `chatProviderNativeToolUtils` support composition/wrapper and Chat inline admission block are deleted and gated against restoration.
- Ordinary Chat request planning and compact admission are Assistant Runtime-owned through `createAssistantConversationRequestPlanningRuntime` and bootstrap-composed `conversationAssistantRequestPlanningRuntime`. Contribution assembly, native-search trace admission, mode-free system-prompt construction, requested-model compact-state lookup, context planning, compact usage/events/traces, required-unsupported blocking, continuity reuse, and local context-pack tracing preserve exact ordering, source references, requested/upstream model identities, and frozen input nonmutation. The planning input, bootstrap generic composition, and `buildSystemPrompt` dependency expose no product-mode discriminator; forged historical mode remains inert caller data and cannot select an Agent or Tavern prompt preset. Reply start forwards no mode into planning. Focused behavior tests and architecture mutation checks prevent restoration in the runtime, bootstrap, prompt builder, or reply-start call.
- Ordinary Chat/Tavern provider dispatch is Assistant Runtime-owned through `createAssistantConversationProviderDispatchRuntime` and bootstrap-composed `conversationAssistantProviderDispatchRuntime`. It records the sanitized running model trace before lifecycle construction, assembles the provider-neutral request from the explicit planned prompt, preserves exact provider/settings/array/controller/signal/compact/tool identities, keeps requested and routed model plus conversation/session identities aligned, prevents lifecycle collisions from replacing authoritative dispatch fields, and propagates exact started/cancelled/failed outcomes and dependency rejections. The target reply-start runtime delegates the bounded dispatch and does not import the concrete streaming runtime or own trace IDs, trace/store projection, request assembly, or stream start; focused behavior coverage and architecture self-test mutations gate those deletions.
- Ordinary Chat provider finalization/failure lifecycle callback construction is Assistant Runtime-owned through `createAssistantConversationStreamLifecycleRuntime` and bootstrap-composed `conversationAssistantStreamLifecycleRuntime`. Completion preserves exact result, context, conversation, provider, trace, search, prompt, message, MCP/provider-tool, workspace, controller, flush, upstream-model, and compact-state identities; completion, provider, and start failures preserve exact error and credential-group identity and delegate once to the existing target finalization/failure runtimes. Chat supplies one immutable lifecycle capture and no longer imports those concrete runtimes or `ProviderRuntimeError`; focused behavior coverage plus architecture self-test mutations gate both the target forwarding contract and the deleted Chat closure.
- Ordinary Chat reply-start sequencing is Assistant Runtime-owned through `createAssistantConversationReplyStartRuntime` and bootstrap-composed `conversationAssistantReplyStartRuntime`. It preserves reply-session -> provider-admission -> latest-reread -> single conversation-workspace source resolution/admission -> plain-Chat-handoff or full context -> provider-tool -> request-planning -> provider-dispatch/lifecycle ordering, two distinct settings reads, exact controller/signal/store-object/array/tool/workspace identities, typed terminal outcomes for every short-circuit, and dependency rejection propagation. Authoritative `none` preserves the plain-Chat path with no run allocation; `ready` skips plain Chat, supplies exact frozen workspace context through context acquisition and request planning, and then carries only the exact bound handoff into durable dispatch, stream lifecycle, and finalization. `conversationReplyStart` and the historical Agent fallback consume `startConversationAssistantReplyAfterHistoryProjection`; startup installs the stream aborter explicitly before binding presentation commands. Focused Chat, historical Agent fallback, retry/regenerate, context-cancellation, MCP, mode-independence, and workspace compatibility coverage passed before `src/services/chatRunner.ts`, the obsolete legacy starter name, Tavern-named source/context-acquisition APIs, and the historical writeback-only Tavern runtime/bootstrap were deleted; architecture gates forbid restoration.
- Remote-compact state reuse and completed/failed outcome recording are Providers-owned through a dependency-injected lifecycle. Bootstrap binds compact usage, scoped state persistence, runtime events, and the clock; Assistant Runtime forwards the exact provider identity and resolved Responses-route evidence through reply start, stream lifecycle, and finalization. Reuse fails closed before repository access unless all three OpenAI Responses evidence fields match, and completed active-state writes require the same exact triple on both lifecycle input and the recorded usage. An admitted repository lookup remains fail-open on storage failure. Finalization preserves upstream-model usage identity, pre-success outcome ordering, failed-state diagnostics, and bounded raw-text-free fragment identity projection. Missing, malformed, ambiguous, Anthropic, local, and failed completion evidence cannot create reusable active state; omitted evidence remains literal in compatibility usage/events instead of being upgraded, and failed-state diagnostics remain compatible. The five private `chatRunner` helpers and `chatRemoteCompactUtils.ts` are deleted and gated against restoration.
- Post-reply memory-extraction admission, running/completed/cancelled/failed lifecycle, bounded added-item projection, and non-fatal failure classification are Knowledge-owned through a dependency-injected runtime. Bootstrap binds the existing provider-backed extractor, current settings read, localized trace copy, clock/identity, and completion projection; the Assistant Runtime finalizer starts extraction after terminal trace settlement with a request-detached signal, observes rejection without an unhandled promise, and routes traces through the existing sanitized projection. Disabled-before-provider admission, exact message/provider/model/signal identity, late request-cancellation isolation with live terminal metadata, successful empty extraction, three-item/full-count output, strict `AbortError` classification, nonmutation, and durable device persistence remain covered. The private `extractMemoriesWithTrace` helper and direct Knowledge extractor import are removed from `chatRunner` and gated against restoration. A bootstrap-owned detached-work registry keys each lease by conversation and assistant message, cancels same-key replacements, protects against stale releases, and supports conversation deletion, clear/import replacement, and root bootstrap teardown; finalization now requires that injected lease boundary, and focused registry, finalizer, store, bootstrap, and architecture coverage forbids restoration of an instance-local fallback.
- Initial ordinary Chat knowledge-context admission is Assistant Runtime-owned through a dependency-injected runtime. Bootstrap binds the existing Knowledge retriever, localized trace and store projection, clock/identity, and cancellation classification. Exact conversation/user/signal identity, running-before-retrieval order, source counts, ordered RAG/memory/knowledge traces, no-user skip, non-fatal failure, pre/post-I/O cancellation, post-outcome cancellation races, and frozen-input nonmutation remain covered. Cancellation returns before web, MCP, or provider work; the inline `chatRunner` initial Knowledge lifecycle and direct initial retrieval call are removed and gated against restoration.
- Post-generation verification, supplemental FLARE evidence projection, citation merge, and RAG evaluation are Knowledge-owned through one staged dependency-injected runtime. Bootstrap binds the existing verifier, concrete FLARE retriever, evaluation repository, localized sanitized traces, clock, and identity. The Assistant Runtime finalizer keeps verification before provider/MCP revisions, supplemental retrieval after revisions, and terminal success/Tavern writeback/provider-health projection before evaluation. Exact conversation/query/follow-up/exclusion/four-result/signal identity, first-answer retention, ordered source/citation deduplication, raw trace projection, successful empty retrieval, pre-abort skips, legacy non-fatal mid-FLARE `AbortError`, observed fire-and-forget evaluation rejection, quality/default projection, and frozen-input nonmutation are covered. Direct verifier/retriever/evaluation/deduplication dependencies and inline Knowledge trace blocks are removed from `chatRunner` and gated against restoration.
- The ordinary external-web pre-search runtime and bootstrap binding are deleted and restoration-gated. Ordinary Chat startup neither synthesizes a search query nor fetches a fixed result count. Integrations retains provider resolution and configured `search_web` execution, while workflow and provider-tool admission expose the canonical built-in only when effective settings permit it; the model decides whether and when to call it. Tool execution owns cancellation, bounded receipts, and visible traces or failures.
- Ordinary Chat context acquisition is Assistant Runtime-owned through `createAssistantConversationContextAcquisitionRuntime` and bootstrap-composed `conversationAssistantContextAcquisitionRuntime`. Reply start supplies the already-admitted optional frozen workspace context; acquisition performs no workspace repository read and has no product-mode gate. Fail-open configured-provider lookup, fresh Knowledge settings, exact workspace-context identity, effective search-mode resolution, creation of an exact empty initial web-source list, exact-signal MCP admission, typed cancellation/failure, and ordered MCP trace projection remain covered. It never searches from the latest-user query; `search_web` remains a separately admitted model-directed tool. Chat delegates one typed acquisition and no longer imports or calls the covered Knowledge, workspace-source, removed web-presearch, MCP, search-policy, or MCP-tag dependencies; the inline multi-lane acquisition block, `resolveTavernContext`, and raw/projection Tavern context APIs are deleted and restoration-gated.
- Tagged MCP Chat/Tavern tool-turn orchestration is Assistant Runtime-owned, while bounded request parsing plus cancellable catalog, enabled-tool identity, connected/offline admission, and provider prompt construction are Integrations-owned and consumed through its public API. Catalog listing receives the exact request signal; pre/post-I/O cancellation suppresses downstream effects; dependency rejection becomes typed `server_listing_failed/unknown`; and connected-only executable tools preserve qualified and unique-bare selection, stable order, exact prompt text, and schema truncation. Bootstrap owns localized trace projection. Stable selected/requested task identity, exact arguments/signal/mode propagation, destructive confirmation metadata, post-task cancellation order, copied block truncation, refreshed settings, non-fatal failures, and Chat/Companion parity remain covered. `chatMcpToolRuntime.ts`, `mcpToolRequest.ts`, `chatMcpContextUtils.ts`, and `chatMcpToolIdentityUtils.ts` are deleted and forbidden from returning; revision-message shaping and shared result formatting remain separate compatibility boundaries.
- Provider-native Chat model-operation admission, durable one-call execution, persisted confirmation resumption, replay synthesis, cancellation, output suppression, and trace planning are Assistant Runtime-owned through dependency-injected runtimes. Bootstrap binds declarations, mode-free tool-catalog discovery, task execution, RAG, generation parameters, streaming synthesis, and localized trace projection. Internal and external task dispatch use mode-free policy options with exact run and cancellation identity; pending attempts and approved confirmation replay retain that identity, while internal operations cannot fall through to the external executor. Catalog discovery itself accepts no mode. The policy/audit context is persistence-free: durable task identity stores the exact tool, idempotency key, optional Chat `AssistantRunId`, frozen catalog revision, argument digest, call identity, and index, not a product mode. The immutable catalog admits at most 64 declarations within the configured permission ceiling and executes none when one model turn requests multiple calls. Provider arguments cross the JSON validator without catch-to-empty coercion, and every real observation returns through model continuation even when pre-tool prose is substantive. A reconstructed catalog-bound session validates the exact run/call/catalog/task identity plus the frozen request, protocol mode, pre-tool output, and continuation state before persisted approval or decline is journaled. Pre-aborted cancellation performs no session/provider work; concurrent, tampered, and terminal replay fail closed; terminal transitions clear pending state; terminal task receipts are reused without another executor; and post-effect cancellation cannot reroute or continue the model. Atomic run/journal persistence plus durable task idempotency remain the cross-runtime replay boundary. Native and structured fallback parity, signal/task identity, step-limit evidence, provider replay payloads, same-task confirmation/rejection, native-search/internal-output suppression, runtime-log propagation, and task rejection remain covered. The private `chatRunner` cluster and `agentToolTaskRuntime` path plus its eight Agent-named APIs are removed and restoration-gated. `taskBoundToolRuntime` is the sole bootstrap task/tool execution gateway; historical trace vocabulary and mode decoders remain compatibility data, while Provider declaration/support utilities remain a separate Providers/bootstrap boundary.
- Provider-native tool catalog discovery, admission, admitted context, and durable task options are mode-free. Stable conversation/message/call/catalog/argument identity and the exact cancellation signal cross the Tasks boundary; Assistant Runtime cannot manufacture Agent or Companion authority or a synthetic `AssistantRunId` from caller data. Focused behavior and architecture restoration gates protect that separation.
- Typed Chat is model-first. `conversationMessageCommand` projects the normalized user turn exactly once and dispatches it through Assistant Runtime; `rag`, settings/theme phrases, files, diagnostics, topic switching, source-code requests, Android-like text, and workflow phrases cannot select local execution or synthesize an assistant response. Explicit UI workflow IDs, requested-output controls, cancellation, confirmation, and undo receipts remain structured inputs. The local Settings parser, its package script, and theme/provider test dependencies are deleted; `settingsActionCommand` remains only for explicit Settings controls and task-bound canonical Settings operations.
- Ordinary Chat provider-tool support, declaration admission, manifest/unclaimed-skip trace projection, nonblocking compatibility logging, and tool-gateway outcome/trace projection are Assistant Runtime-owned through `createAssistantConversationProviderToolAdmissionRuntime` and bootstrap-composed `conversationAssistantProviderToolAdmissionRuntime`. The runtime and its injected admission port accept no product-mode field, and bootstrap imports no Workspaces mode contract; forged Agent or Companion caller data is inert. Catalog discovery, the admitted provider-tool context, and durable Tasks options are mode-free; Tasks also discard a legacy mode field before policy or executor work. The Integrations policy strips historical `supportedModes` and `modePolicyReason` before publication. Exact provider/model/settings/wire identities, upstream/requested model distinction, pending-log behavior, rejection suppression, gateway prompt/count/support/context/options, ordering, and frozen-input nonmutation remain covered. Chat consumes one typed outcome; its inline provider-tool admission/gateway block and direct capability, trace, runtime-log, diagnostics, gateway, and provider-tool-turn composition imports are removed and restoration-gated. No cancellation gate is added at this seam.
- Terminal assistant-message success and failure projection is Conversations-owned through current Chat-only dependency-injected plans. Bootstrap applies store, streaming, task, and error effects without product-mode resolution. The duplicate legacy mutation helpers are deleted and restoration-gated.
- Historical conversation-workspace post-reply fallback is removed. `writeBackLegacyWorkspaceReply`, the writeback-only Tavern raw/projection runtimes, their bootstrap composition, raw/projection `resolveContext` APIs, context cancellation wiring, Chat-local outcome/metadata/writeback helpers, inline Tavern trace blocks, and direct raw-runtime calls are deleted and restoration-gated. Finalization accepts only the exact durable workspace handoff; legacy v1-v3 rows without one are terminal no-replay inputs.
- Workspaces exports the current generic Chat workspace source/writeback/review contracts, SQLite/browser-v2 atomic stores, and bounded receipt lookups. The browser repository strictly reads and writes v2. Bootstrap selects SQLite on React Native or browser v2 under Web Locks. Tavern v1 upgrade, migration source/target ports, missing-port migration predicates, and `source_receipts_unmigrated` are removed.
- Ordinary Chat/Tavern provider-stream dispatch, callback routing, active-handle replacement, cancellation, settlement, and text/trace projection are Assistant Runtime-owned through dependency-injected runtimes. Bootstrap binds provider dispatch, stores, projection thresholds, citation updates, and stream lifecycle. Exact request/signal/result/citation/trace identity, original-controller registration before dispatch, installed 64 ms/128-character text and 280 ms/eight-entry trace thresholds, provider/completion failure cleanup order, returned-handle cancellation, rejected-settlement observation, replacement-stream preservation, and input nonmutation remain covered. Direct `streamProviderChat`, `ProviderRuntimeStreamHandle`, and stream-projection-policy dependencies plus the inline callback/handle bridge are removed from `chatRunner` and gated against restoration; the duplicate `chatStreamingBuffers.ts` service remains deleted.
- Provider start and completion failure projection is Assistant Runtime-owned through the current Chat-only stream failure runtime. Error-code precedence, cancellation, trace projection, and terminal identity remain covered without product-mode resolution.
- Recoverable conversation failures use one ephemeral Chat error channel. New-turn clearing, model validation, startup, retry/regenerate, terminal projection, and the status banner share that value; no historical conversation mode is read or projected.
- Ordinary Chat/Tavern success and provider-failure finalization is Assistant Runtime-owned through `createAssistantConversationFinalizationRuntime` and bootstrap-composed `conversationAssistantFinalizationRuntime`. It preserves exact lifecycle controller/flush identity, matching-stream replacement safety, typed skipped/completed outcomes, verification -> provider revision -> MCP revision -> supplemental evidence, remote-compact outcome before commit, commit -> awaited generic workspace writeback -> authoritative structural projection -> nonblocking settings health -> evaluation, model/native/general trace settlement before committed reread and nonblocking memory extraction, and provider-failure model -> optional native -> terminal -> nonblocking unhealthy projection. Exact handoff/output/signal identity, authoritative outcome handling, rejection identity, compact fallback accounting, trace values, cancellation, and nonmutation are focused-gated. Workspace context is planning-only and cannot enter lifecycle/finalization; no outcome, including synthetic unavailability, can invoke a second mutation path. The inline Chat finalizer, direct finalization-lane imports, local settlement block, and historical Tavern fallback are deleted and forbidden from returning.
- Chat message packing, structured local compression, context window/fragment planning, stable assembly manifests and cache diagnostics, remote/local compact admission, fallback/blocking state, and redacted runtime-event projection are Assistant Runtime-owned through dependency-injected policies. Bootstrap binds the existing message/text token estimators, provider/model reasoning reserve, provider remote-compact decision, and runtime-event sink. Filtering, attachment-only admission, budget arithmetic, source caps/order, reference preservation, single-message truncation, schema-v2 metadata, fragment reuse, failure projection, determinism, and nonmutation remain covered across Chat and the isolated Tavern contribution lane. `contextPacker.ts` and `contextPlanner.ts` are deleted and gated.
- Workflow-skill suggestion, preview, visible approval, persistence, idempotency/conflict handling, enabled/review/tag/edit lifecycle, reusable-run sanitization, RAG requirement policy, and runtime snapshot admission are Tasks-owned. Policy, lifecycle/state, selection/blocking types and methods are Chat-neutral from `src/modules/tasks/application/workflowSkillPolicy.ts` through `src/bootstrap/workflowSkills.ts`, Conversations, Settings, plugin projection, and composer selection; focused coverage preserves approval, nonmutation, exact control/schema/output markers, stable `not_agent_workflow`, disabled listing, manifest-gated enablement, edit review reset, ordinary-skill selectability, and blocked-state precedence. Chat now emits the canonical `focus=workflow` settings route, the Settings presentation uses Workflow-neutral type/helper names and visible labels, and the one historical `focus=agent-workflow` route input is normalized immediately to `workflow`. The former Agent-named policy/factory/runtime-block/snapshot APIs, runtime option fields, Tasks file, bootstrap file, lifecycle/state aliases, and legacy `src/services/agent/agentWorkflowSkills.ts` facade/service-barrel export are deleted and restoration-gated. The historical route input, persisted `agentWorkflow*` settings, translation keys, underlying execution-definition and checkpoint contracts, workflow schemas, and historical traces remain compatibility until their migration evidence permits deletion.
- Chat workflow-entry routing and workflow reply projection are Tasks-owned through `conversationChatWorkflowEntryPolicy`. `@/bootstrap/conversationChatWorkflowEntry` binds the canonical intent classifier, Tasks-owned orchestrator, and workflow-skill suggestion producer through Chat-neutral adapter names; exact routing precedence, cancellation, skipped text, run input/status/failure mapping, completion-trace suggestion attachment, and nonmutation remain covered. The former Agent-named Tasks policy file, bootstrap adapter, service facade, and Agent service barrel are deleted and restoration-gated.
- Chat workflow runtime selection, workflow-skill blocking, Android workflow matching, search deferral, RAG adaptation, and assistant-message resolution are Tasks-owned and receive no product-mode field. Bootstrap binds current Chat-only policies, catalogs, Knowledge, search, clock, and trace dependencies. Mode-specific Tasks/bootstrap/service paths are deleted and restoration-gated.
- Conversation tool-catalog assembly is bootstrap-owned over the neutral Integrations catalog policy. Dynamic MCP, canonical built-in, internal RAG/work-artifact, Android, static review, and unique-resolution consumers share `@/bootstrap/conversationToolCatalog`; neither dynamic nor static listing accepts or synthesizes a product mode. `ConversationToolCatalog*`, `ListConversationToolCatalogOptions`, `listConversationToolCatalog`, `listStaticConversationToolCatalog`, `ListConversationToolManifestOptions`, `listConversationToolManifests`, `listStaticConversationToolManifests`, `resolveConversationTool`, `resolveManifestExecutionPolicy`, and `annotateManifestExecutionPolicy` are the active catalog/policy names. Neutral annotation derives risk, confirmation, and output-boundary metadata while removing historical `supportedModes` and `modePolicyReason` and preserving unrelated metadata. The two `agentToolCatalog.ts` paths, every Agent-named catalog API, `agentToolRegistry.ts`, its service-barrel export, and Workspaces' annotation/filter/availability/mode-policy helpers are deleted and restoration-gated. No production serializer writes the removed mode fields, so no bulk data migration is required. Manifest IDs, source values, order, schemas, hashes, and historical trace/confirmation/idempotency literals remain unchanged.
- Work-artifact workflow shaping, validation, and fail-closed JSON admission are Integrations-owned through a dependency-injected policy. Bootstrap supplies the existing summarizer, quality audit, redaction, and sensitive-text detector; citation/source normalization, compact bounds, quality-gap order/deduplication, exact validation error order, deterministic behavior, and nonmutation remain covered. `workArtifactWorkflow.ts` and its service-barrel export are deleted and gated.
- Provider-native Chat RAG now propagates the exact cancellation signal through both fallback and agentic Knowledge retrieval calls. The focused Agent RAG gate prevents the adapter and Chat consumer from dropping the signal.
- Conversation RAG runtime adaptation is Knowledge-owned through `ConversationRagRuntime` and `createConversationRagRuntime`. The structural adapter maps request-over-default title/prompt precedence, profile/reason, immutable memory/retrieval inputs, budgets, clock, and exact cancellation into the existing agentic retrieval algorithm; Chat workflow and provider-native Chat consume the target entry. Parity, callback signal identity, pre-abort no-work behavior, and nonmutation remain covered; both Agent-named `agentRagRuntime.ts` paths, their factory/contracts, and the obsolete Agent service barrel are deleted and gated.
- Chat-neutral workflow tool-call trace construction, inference, validation, shape comparison, tagged-block stripping, and raw-request detection are Tasks-owned through `workflowToolCallTracePolicy` and composed by `bootstrap/workflowToolCallTrace`. Bootstrap supplies the existing redaction and metadata sanitizer, and provider/Chat/MCP/security consumers use only the neutral binding. Contract/error order, status/index/count limits, 160-character bounds, secret redaction, legacy shape equivalence, custom tag escaping, malformed/raw JSON handling, and nonmutation remain covered. The former `agentToolCallTracePolicy` Tasks path, `bootstrap/agentToolCallTrace`, every Agent-named public trace-policy symbol, `agentToolCallTrace.ts`, and its service-barrel export are deleted and restoration-gated; the exact `islemind.agent.tool-call-trace.v1` value and Agent validation strings remain compatibility data.
- Workflow search-tool admission, provider-native subset selection, and exact built-in search request admission are Tasks-owned through `createWorkflowSearchToolAdmissionPolicy` and composed by `bootstrap/workflowSearchToolAdmission`. Chat reply startup, provider-native tool turns, model-operation catalog discovery, and Chat workflow resolution consume the same binding. Native/off suppression, canonical `toolId` precedence, bounded source/server/name fallback matching, ordering, unchanged-array identity, frozen-input nonmutation, configured permission ceilings, and the post-filter 64-declaration fail-closed limit remain covered. The former `agentSearchToolAdmissionPolicy.ts`, `bootstrap/agentSearchToolAdmission.ts`, Agent-named public symbols, and `agentSearchToolPolicy.ts` service are deleted and restoration-gated. Persisted `agentWorkflowAllowReadOnlyTools`, `agentWorkflowAllowReadWriteTools`, and `agentWorkflowAllowDestructiveTools` keys remain compatibility inputs until Settings persistence and every consumer forward-migrate with focused replacement coverage.
- Provider-native Agent tool declaration shaping is Providers-owned through a structural dependency-injected adapter. Bootstrap supplies trace redaction and schema sanitization; Chat, canonical Agent security gates, and tool-gateway contracts use target values/types. OpenAI Chat/Responses, Anthropic, and Google shapes, exact provider routing, ordered enabled/permission/max admission, deterministic unique bounded names, bounded/redacted descriptions and schemas, skip audits, and nonmutation remain covered; `agentProviderToolAdapter.ts` and its service-barrel export are deleted and gated.
- Provider conformance, model-access, payload, session-lease, request-hardening, remote-compaction policy, compact-usage state, and durable scoped compact-state persistence are target-owned and bootstrap-composed; their legacy facades and duplicate helpers are deleted.
- Provider fallback decision and route-health effects are injected from bootstrap. Current and legacy bounded/redacted decision evidence, health classification, retry-after state, exact cancellation reason through timeout transport, and no post-abort completion or false selected-route health remain covered; `providerRuntimeFallbackLogging.ts` is deleted.
- Provider runtime request/result and callback/stream contracts are provider-owned and exported through `@/modules/providers`; `src/services/ai/providerRuntimeContracts.ts` is deleted and bootstrap/pipeline/executor consumers use the target entry.
- Responses WebSocket transport behavior is provider-owned and bootstrap-composed. Pre-abort creates no socket, arbitrary cancellation reasons retain identity, queued callbacks are inert after cancellation, and the legacy service transport is deleted. Stream preparation now rechecks cancellation after preparation before emitting a ready outcome or dispatching upstream work.
- Provider gateway outcome projection and request-preparation/admission sequencing are bootstrap-owned through `providerRuntimeGateway.ts` and `providerRuntimePipeline.ts`. Their service files are deleted and gated; exact cancellation, alias/affinity order, policy stages, Bedrock preparation, bounded route snapshots, and typed gateway events remain covered.
- Provider runtime execution and diagnostics are bootstrap-owned through `providerRuntimeExecutor.ts` and `providerRuntimeDiagnostics.ts`. Their service files are deleted and gated; HTTP/SSE and WebSocket behavior, retry/rectification/fallback, exact cancellation reasons, post-abort callback suppression, selected-route health effects, and typed redacted evidence remain covered.
- Provider activation patch buffering is provider-owned. Batch activation propagates its exact signal through hydration and persistence, prevents queued post-cancellation writes, preserves serialized last-write-wins overlays and credential health, captures timer failures for awaited retry, and disposes lifecycle state; the legacy patch-buffer service is deleted. Persistence already executing in a non-cancellable adapter may still complete.
- Knowledge local-model availability/capability policy, import, retrieval, RAG, persistence, portable snapshots, and replay are target-owned; the covered context/RAG/local-model-rules facades are deleted.
- Local-model artifact admission and bounded streaming SHA-256 are target/platform-owned and bootstrap-composed. Model downloads, downloaded-model verification, and APK update verification share the binding; the duplicated service checksum cluster is deleted.
- Local-model official/mirror download, progress, cancellation, stale cleanup, staged atomic publish, rollback, and installed-directory deletion are target/platform-owned. The service download transaction and direct Expo filesystem helpers are deleted while catalog/state compatibility remains.
- Local-model records and failure state now use a bounded, validated, cancellation-aware knowledge repository through an AsyncStorage platform adapter and bootstrap singleton. Direct AsyncStorage state, module-global cache, and manual mutation helpers are deleted from the legacy service; only operational/UI compatibility wrappers remain.
- Local-model catalog parsing, bundle validation, installed-state reconciliation, view projection, deterministic cache identity, downloaded/bundled availability checks, and active fallback selection are knowledge-owned and bootstrap-composed. The service no longer imports raw catalog/bundle/platform inputs or owns duplicate selection helpers; cancelled installs no longer record false verification failures.
- Local-model platform adapters no longer import knowledge module contracts; bootstrap composes structurally compatible platform ports, and the architecture boundary gate is clear.
- Local-model operational composition is exposed through `@/bootstrap/localModelRuntime`. Settings, storage reset, and ONNX composition use it directly with exact optional cancellation; the final `localEmbeddingModels.ts` compatibility facade and the consumer-free `lazyEmbedding.ts` failure-only shell are deleted and restoration-gated.
- The consumer-free `src/services/rag/vectorOptimization.ts` LSH/TopK experiment and its isolated Jest file are deleted. Production Knowledge retrieval, SQLite vector indexes, embedding records, and persisted schemas never imported them, so no data migration is required.
- Consumer-free UI forwarding files, the duplicate message comparator, the detached provider-activation banner, unused typography/performance/SQLite/streaming/readiness helpers, and the two obsolete optimization-presence scripts are deleted. The mounted Isle UI exports, `MessageBubble` comparator, inline provider activation card, production SQLite adapters, Markdown renderer, and search policy remain unchanged; these deletions touch no persisted record or schema.
- Workflow checkpoints are Tasks-owned, strictly validated, append-only, and SQLite-backed through a narrow cancellation-aware bootstrap bridge. Neutral payloads use `islemind.workflow-checkpoint.v2` and `islemind.workflow-checkpoint-journal.v2`; SQLite uses `workflow_checkpoints`, `workflow_checkpoint_journal`, and `workflow_checkpoint_storage` with the exact `islemind.workflow-checkpoint-storage.v2` ready marker. Per-run journal storage and recovery reads retain a bounded 16-snapshot tail whose entries are self-contained recovery anchors; older rows are pruned transactionally without enabling side-effect replay. Chat-owned workflow execution injects the store through bootstrap composition; planning, running/progress, waiting, success, failure, and cancellation persistence attempts use the caller signal, legacy `done`/`error` map explicitly to `succeeded`/`failed`, and recovery returns `replaySideEffects: false`. Unsafe nested metadata accessors/proxies fail closed as invalid records. Checkpoint persistence failures fail the parent Chat workflow run instead of allowing silent success. An already-aborted signal cannot create a post-cancellation write, so recovery retains the last committed checkpoint and requires reconciliation instead of replaying side effects.
- Workflow checkpoint projection is Tasks-owned through the Chat-neutral `createWorkflowCheckpointProjectionSession` API. New v2 checkpoints emit only `workflow-goal-*`; the strict generic parser still accepts bounded historical `agent-goal-*` values unchanged as opaque read-only compatibility data, and no current writer emits that prefix. The legacy service adapter and former Agent-named session path plus all nine Agent-named projection exports are deleted and restoration-gated, and a mutation gate rejects restoration of Agent-prefixed writes. The Tasks orchestrator consumes the generic session dependency; bootstrap injects time, redaction, and the durable store. Focused coverage protects exact status mapping, neutral new-write and legacy-read goal hashing, redaction, bounded task/evidence/pending/failure projection, caller signal identity, fail-closed persistence, parent Chat failure, and recovery without side-effect replay.
- Workflow checkpoint revision and append-only journal recording are Tasks-owned through the Chat-neutral `createWorkflowCheckpointRecorder` API. The projection session consumes the generic recorder directly; the former Agent-named recorder path and all four Agent-named recorder exports are deleted and restoration-gated. Focused lifecycle and bounded-journal coverage preserves exact v2 schemas, revisions, sequences, monotonic timestamps, pending/failure clearing, completed-step append semantics, signal identity, and `replaySideEffects: false` recovery. Historical `agent-*` identities and visible trace/error copy remain record content only; they no longer name checkpoint contracts or storage.
- SQLite workflow-checkpoint persistence is publicly exposed through `createSqliteWorkflowCheckpointRepository` and the five neutral `WorkflowCheckpointDatabase*` bridge contracts. The Tasks barrel, cancellation-aware `workflowCheckpoints` bootstrap bridge, and concrete bounded-journal fixture consume only those neutral adapter names; bootstrap publicly exposes `createWorkflowCheckpointRuntime`. Initialization discovers and validates storage inside the caller-signal transaction. Fresh databases create only v2 storage; v1-only databases are strictly decoded and transformed without changing identity, revision, journal sequence, or recovery authority; equivalent dual-family copies reconcile, while one-sided, unknown-version, or divergent copies fail closed. Corrupt current rows and corrupt journal suffixes retain only the same last-safe recovery prefix, while an invalid first retained anchor fails. The complete v2 dataset is reread before the v1 tables are dropped and the ready marker commits, so cancellation or any failure rolls back all DDL and data. The former Agent-named adapter/bootstrap paths and exports are deleted and restoration-gated. Private v1 decoders and table discovery remain required for upgrades until legacy database support ends.
- Workflow checkpoint store orchestration is Chat-neutral through `WorkflowCheckpointRepository`, `WorkflowCheckpointStore`, and `createWorkflowCheckpointStore` in `workflowCheckpoint.ts`. Tasks application policies, Conversations, bootstrap, and the concrete/runtime fixtures consume only neutral v2 public names; the Agent-named codec path, record/repository/store exports, schemas/constants, parsers, validators, and factory are deleted and restoration-gated. Focused persistence, pre-cancellation, recovery-disposition, workflow-run, close/reopen migration, and compatibility coverage preserves repository calls, validation, conflict behavior, and no-replay authority.
- Passive workflow-checkpoint startup reconciliation is Tasks-owned through `createWorkflowCheckpointRecoveryCoordinator` and bootstrap-composed through `recoverVNextWorkflowCheckpoints`. It scans only the exact ordered run IDs returned by unified Chat recovery, preserves the caller signal, skips duplicates, treats `not_found` as an ordinary-Chat outcome, and exposes frozen payload-free observations for every valid disposition. Mismatched identity, replay authority, status/disposition drift, corruption, and thrown persistence fail closed; cancellation stops the scan. The coordinator has no task, executor, provider, resume, or checkpoint-mutation capability. Focused tests cover all dispositions, exact signal/run association, no-call pre-abort, authoritative post-read cancellation, duplicate suppression, hostile recovery shapes, raw-message exclusion, immutable reports, disk-backed v1-to-v2 close/reopen and idempotent reopen, pruned tails, corruption fallback, unknown versions, equivalent/divergent dual families, cancellation rollback, CAS, and `replaySideEffects: false`. Only the repository-private v1 upgrade decoder/table discovery remain; their deletion requires an explicit end to legacy database support.
- Conversation tool catalog manifest/source contracts are Integrations-owned; workflow request, pending-action, observation, run, runtime-log, limits, failure, and definition contracts are Tasks-owned; conversation-neutral RAG runtime contracts are Knowledge-owned; requested-output is Workspaces-owned. Production consumers use the owning public entries directly, and `agentToolTypes.ts` plus its service-barrel export are deleted and gated.
- Conversation-bound transient activity is now Tasks-owned through the public `conversationTaskActivityRuntime`. Starts accept only `chat-turn | chat-workflow`, require exact conversation/message identity, use mode-neutral IDs, isolate subscribers, preserve terminal idempotency and newest-first ordering, expire inactive records after 30 minutes, and cap terminal history at 120 with 24 metadata fields. Assistant reply sessions, structured workflows, terminal projection, conversation control, and Chat presentation consume the public API. `projectConversationTaskStatus` still selects the exact `conversationId` and every `queued | running | awaiting-confirmation` structural record regardless of historical kind/mode, so persisted compatibility fixtures remain readable without granting execution authority. The ProductMode task registry, Workspaces queue lookup, low-disruption/mode fields, cross-mode snapshots/filtering, unused update API, ProductMode/Agent presentation names, and Agent-only task-card translations are deleted and restoration-gated. The registry remains observational and realm-local. Linked streaming cancellation aborts Chat. An active non-streaming `chat-workflow` now binds exact activity conversation/message identity to the same live durable Chat `AssistantRun`; card cancellation rejects mismatches, invokes `runtime.cancel(handle.runId)`, aborts the request controller, awaits terminal durable cancellation, then releases the binding. Unbound activities retain observational-only terminalization and do not claim durable Task cancellation. `test:vnext-conversation-message-command` covers the full identity, journal, abort, terminal, and release sequence.
- Process-trace contracts, recursive redaction, metadata sanitization, exact bounded text, and canonical timestamp/duration/content projection are Core-owned through `@/core`. All production consumers use the Core API; `src/types/traceContracts.ts`, `src/utils/traceSafety.ts`, `src/services/agent/agentTrace.ts`, its service-barrel export, the consumer-free workflow trace validator, and its private audit cluster are deleted. Executable coverage protects timestamp/duration normalization, supplied negative-duration clamping, exact small and zero output limits, nested redaction, nonmutation, tool/non-tool content bounds, and facade non-return.
- Workflow transition, step-count, goal-hash, and bounded diagnostic evidence are Tasks-owned through the Chat-neutral `workflowRuntimePolicy` API. The Tasks-owned orchestrator, completion, observation, failure, pending-action, step-outcome, and run contracts consume only the generic public symbols. The former Agent-named target path and exports plus the service runtime policy are deleted and restoration-gated. New Chat workflow traces emit `islemind.workflow-runtime.v2`, `workflowRuntime*` metadata keys, and neutral transition-error wording. Focused coverage preserves every status/failure/pending/transition literal, same-state transitions, the terminal waiting matrix, exact `fnv1a32-*` goal hash without raw goals, step counters, input immutability, and the last-eight transition window; it also proves historical `islemind.agent.workflow-runtime.v1` and `agentWorkflowRuntime*` trace metadata remains inert readable data without becoming execution authority. No task or checkpoint persistence rewrite is required.
- Workflow step execution is Tasks-owned through the Chat-neutral `createWorkflowStepExecutor` contract composed by the Tasks-owned orchestrator with the Chat-neutral `TaskBoundToolRuntime` port. Exact source, failure, status, and historical-mode literals; no-tool completion; pre-dispatch no-effect cancellation; post-effect success suppression; request, run, option, and signal identity; unavailable-tool and cancellation copy; observation/trace metadata; timestamps; 360/160 bounds; truncation; and hostile accessor/proxy/`toJSON` handling remain compatible. The former Agent-named Tasks step target path, all 13 Agent-named step contracts/factory, the service executor/barrel, and the `agentToolTaskRuntime` bootstrap path plus its eight Agent-named public APIs are deleted and restoration-gated. Provider-native, tagged-MCP, model-operation, and workflow consumers now use the neutral gateway. Persisted `agent:` idempotency keys, `agent-confirm-*` confirmation IDs, Agent-visible trace/copy values, and Agent-named catalog/orchestration/checkpoint contracts remain migration inventory.
- Workflow planning is Tasks-owned through the Chat-neutral `createWorkflowPlanner` contract composed by the Tasks-owned orchestrator. Deterministic IDs/time, explicit-classification precedence, direct-Chat zero-step selection, selected-workflow order and identity, detached runtime argument binding, Android/RAG/search/work-artifact sanitization, exact 3x160 acceptance and 5x120 tool evidence bounds, redaction, and frozen-input nonmutation remain covered. The former Agent-named Tasks planner path, all 12 Agent-named planner contracts/factory, the service planner, and the service barrel are deleted and restoration-gated. The exact `agent-plan-*`, `Agent plan`, `agent-workflow-skill`, intent/source/output literals, and Agent-named workflow definition/orchestrator/checkpoint contracts remain migration inventory.
- Workflow execution-run projection is Tasks-owned through the Chat-neutral `WorkflowExecutionRun` and `WorkflowExecutionRuntimeLogOptions` contracts. The orchestrator and workflow-skill policy consume the same target file, while bootstrap imports it only through `@/modules/tasks`. The former Agent-named run-contract path and its two exported names are deleted and restoration-gated. Field shape, optionality, mutability, cancellation, checkpoint, step, and completion behavior are unchanged. New records use `workflow-run-*`; historical `agent-run-*` identities, Agent-visible copy, checkpoint schemas, and adjacent Agent-named definition and checkpoint contracts remain migration inventory.
- Workflow cancellation progress and step-limit pause construction are Tasks-owned through the Chat-neutral `createWorkflowContinuationPolicy` contract composed in `bootstrap/workflowContinuation`. Done-only cancellation counts, attempted-step limit counts, stable non-confirmable pending-action identity, exact progress/workflow attribution, injected timestamps, bounded redacted copy, and the absence of resume payloads remain covered. The orchestrator consumes the generic binding; the former Agent-named policy path, public contracts/factory/constant, inline bootstrap construction, and service adapters are deleted and restoration-gated. The exact `agent-pending-step-limit-*` identity, `cancelled | waiting | step_limit_reached` states, progress metadata fields, blocked-reason value, hash/bounds, and visible Agent/agentic workflow copy remain live compatibility evidence pending checkpoint, trace, and message-action migration.
- Workflow observation projection is Tasks-owned through the Chat-neutral `createWorkflowObservationPolicy` contract composed in `bootstrap/workflowObservation`. Workflow identity, step/plan progress, tool/source/error attribution, pending-action workflow-before-step context, deduplication, exact 900/160-character bounds, redaction, determinism, and frozen-input nonmutation remain covered. The orchestrator, RAG evidence, pending-action, and completion policies consume the generic binding; the duplicate workflow/step/failure metadata and prompt-context helpers plus former Agent-named policy/bootstrap files and public exports are deleted and restoration-gated. The exact `Agent plan`, `agent-workflow-skill`, failure metadata field names, and prompt labels remain live compatibility evidence pending downstream trace and policy migration.
- Workflow RAG evidence quality admission and repair-pause projection are Tasks-owned through the Chat-neutral `createWorkflowRagEvidencePolicy` contract composed in `bootstrap/workflowRagEvidence`. Output metrics take precedence over diagnostic fallback metadata; missing sources/citations, low confidence, missing evidence, RAG-off handling, repair strategy, workflow/step attribution, redaction, caller output limits with a safe 900-character default, determinism, and frozen-input nonmutation remain covered. The orchestrator consumes the generic binding; its private RAG evidence inspection, prompt, formatting, and repair-strategy helper cluster plus the former Agent-named policy/bootstrap files and public exports are deleted and restoration-gated. The exact `agent-pending-rag-evidence-*` identity, `rag_evidence` intent, repair codes and strategies, and visible repair copy remain live compatibility evidence pending persisted-message and downstream policy migration.
- Workflow pending-action construction and output formatting are Tasks-owned through the Chat-neutral `createWorkflowPendingActionPolicy` contract composed in `bootstrap/workflowPendingAction`. One descriptor-based snapshot preserves valid detached Android operation arrays while accessors, proxy traps, cycles, exotic/unsupported JSON, unsafe identities, sensitive or pre-truncated payloads, and payloads beyond 1200 characters fail closed. Evidence-repair actions remain non-confirmable with matching `blockedReason`/`repairNextStep`; fixed-clock IDs, localization, exact bounds, step attribution, determinism, frozen-input nonmutation, step-outcome waiting projection, and terminal-trace pending-action identity are covered. The orchestrator consumes the generic binding; its private pending-action, prompt, Android-copy, preview, resume, and serialization helpers plus the former Agent-named policy path, public contracts/factory, and bootstrap binding are deleted and restoration-gated. The exact `agent-pending-*` identity, `permission_required | evidence_insufficient` reasons, `resumeToolRequest`, confirmation authority, metadata precedence, Android localization keys, visible Agent copy, and 1200/900/360/240 bounds remain live compatibility evidence pending runtime, checkpoint, and message-action migration.
- Android workflow runtime-state carry-forward is Tasks-owned through the Chat-neutral `androidWorkflowRuntimeStatePolicy` public API. `bindAndroidWorkflowRuntimeState`, `extractAndroidWorkflowRuntimeState`, and `mergeAndroidWorkflowRuntimeState` preserve dotted and namespaced identities, successful object-shaped observation admission, explicit `operations` precedence over `operationPreview`, missing-only argument filling, source-state precedence, detached nested arrays, and fail-closed malformed or hostile inputs. The Tasks orchestrator and bootstrap composition consume only the neutral contracts. The former `agentAndroidWorkflowRuntimeStatePolicy` path and its seven Agent-named exports are deleted and restoration-gated; because the state is scoped to one in-memory workflow run, no persistence or device migration is required.
- The seven-definition Android workflow catalog is Tasks-owned through `androidWorkflowCatalog.ts`, `AndroidWorkflowCatalogDependencies`, `AndroidWorkflowCatalog`, and `createAndroidWorkflowCatalog`; bootstrap, Chat workflow resolution, and Settings consume only `androidWorkflowCatalog`. The former Agent-named Tasks/bootstrap paths and source symbols are deleted and restoration-gated. Exact definition order, requests, permissions, acceptance checks, sanitizers, timestamps, contract hash, and persisted `agent-workflow-android-*` IDs remain unchanged compatibility data.
- Workflow step permission-evidence projection is Tasks-owned through the Chat-neutral `createWorkflowPermissionEvidencePolicy` contract composed in `bootstrap/workflowPermissionEvidence`. Stable plan, intent, visible-request, tool, workflow, acceptance, and prior-observation sources retain their exact order and deduplication; the visible basis summary remains redacted and bounded to 320 characters without exposing the transport truncation suffix. Frozen plans/workflows, absent optional attribution, and deterministic output are covered. The orchestrator consumes the generic binding; its duplicate evidence builder plus unused content/goal plumbing, former Agent-named policy/bootstrap files, and Agent-named public exports are deleted and restoration-gated. The exact `agent-plan:*`, `source:visible-agent-request`, and `visible Agent plan` values remain live compatibility evidence pending downstream diagnostic and persisted-record migration.
- Android undo follow-up construction, visible output copy, and trace metadata are Tasks-owned through the Chat-neutral `workflowAndroidUndoFollowUpPolicy` public API. `buildWorkflowAndroidUndoFollowUp`, `appendWorkflowAndroidUndoFollowUp`, and `projectWorkflowAndroidUndoFollowUpMetadata` preserve first-valid non-empty `undoOperations` receipt selection; malformed, primitive, top-level-array, empty, accessor, proxy, and other unsafe parsed values fail closed. Exact `android.files.undo_operations` identity, visible-confirmation requirement, summary/copy, successful-output-only guidance, error-trace metadata, and frozen-input nonmutation remain covered. The completion policy and Tasks barrel consume only the neutral contracts; the former `agentAndroidUndoFollowUpPolicy` path and Agent-named exports are deleted and restoration-gated. The `androidUndo*` metadata keys and success/error output asymmetry remain compatibility data.
- Workflow failure output, recovery guidance, tool/source detail attribution, and Android partial-failure rollback projection are Tasks-owned through the Chat-neutral `createWorkflowFailurePolicy` contract composed in `bootstrap/workflowFailure`. Visible next-step precedence, descriptor-safe snapshots, exact redaction/clamping limits, partial-failure counts, and rollback guidance remain covered. The orchestrator and completion policy consume the generic binding; the former Agent-named failure policy path, exported contracts/factory, bootstrap binding, and helper names are deleted and restoration-gated. Generic `WorkflowRuntimeFailureCode` preserves the exact failure-code values; `Agentic workflow failed.` copy, `Reason:`/`Next step:` formatting, Android labels, and 900/700/160/240 bounds remain live compatibility evidence pending terminal-trace migration.
- Terminal workflow completion/synthesis/final-output projection is Tasks-owned through the Chat-neutral `createWorkflowCompletionPolicy` contract composed in `bootstrap/workflowCompletion`. Terminal runtime transitions, immutable completed runs, six step-status counts, limit metadata, failure next-step preservation, successful Android undo suffix preservation, error-output asymmetry, one injected clock read, and frozen-input nonmutation remain covered. The orchestrator consumes only the neutral bootstrap binding; its duplicate completion mutation, synthesis, status-count, limit, and output-clamping helpers remain deleted. The former Agent-named Tasks and bootstrap paths, all ten Agent-named completion contracts/factory, and old binding names are deleted and restoration-gated. Exact `Agent synthesis` / `Agent workflow` titles, historical `agent-run-*` identities, workflow-runtime schema/status values, Android undo metadata, and checkpoint schemas remain compatibility data; new records use `workflow-run-*`. The recorder boundary is now neutral; durable Agent-named checkpoint store/repository contracts remain the next independent persistence boundary.
- Workflow post-step outcome resolution is Tasks-owned through the Chat-neutral `createWorkflowStepOutcomePolicy` contract composed by the Tasks bootstrap orchestrator. Cancelled-step precedence, `permission_required | evidence_insufficient` waiting projection, exact pending-action and transition-step identity, `repairNextStep` propagation, visible tool-failure projection, Android runtime-state extraction/merge, and frozen-input nonmutation remain covered. The former Agent-named target path, exported contracts/factory, and the orchestrator's duplicate post-step cancellation, waiting, failure, and runtime-state branch cluster are deleted and restoration-gated. Agent-named checkpoint contracts plus their visible and persisted literals remain migration inventory.
- Workflow message-action selection is Tasks-owned through the Chat-neutral `createWorkflowMessageActionPolicy` contract and presentation-owned `workflowMessageActionSelectors` binding. Pending confirmation, evidence repair, workflow recovery, and continuation preserve merged-trace ordering, hidden-trace exclusion, exact tool/source/server/permission identity, bounded detached resume arguments, redaction, hostile-input failure, exact 160/900/360/240/1200 limits, and frozen-input nonmutation. The former Agent-named target/selector paths, contracts, factory, and selector names are deleted and restoration-gated. Historical Agent trace titles, reason/metadata values, visible copy, translation keys, `AgentToolPermission`, and `islemind.agent.work-artifact-workflow.v1` remain compatibility inputs pending persisted trace/message and permission-contract migration.
- Workflow intent classification and deterministic classification trace construction are Tasks-owned. The Chat-neutral `workflowIntentClassifier` bootstrap binding accepts model-produced explicit tool requests and user-selected work-artifact output; ordinary text always remains `plain_chat` with `model-tool-selection`, including Settings, search, RAG, diagnostic, and Android wording. Explicit `workflowId` selects a validated workflow, while runtime content and trigger hints no longer select tools or built-in Android workflows before provider/model-operation execution. Alarm/reminder parameter parsing remains available only after an explicit workflow has been selected. The Agent-named target module and bootstrap binding, legacy service classifier, and service barrel export are deleted and restoration-gated; historical `agent-intent-*` IDs and the `Agent intent` trace title remain readable compatibility values.
- Persisted reusable workflow definitions are Tasks-owned through the neutral `workflowDefinitionPolicy` and `bootstrap/workflowDefinitions`. Exact legacy `islemind.agent.workflow.v1` and current `islemind.workflow.v2` inputs dual-read into deeply detached frozen v2 records with source-schema/rewrite metadata; all new creation, approved saves, serialization, Android templates, presentation projection, and plugin projection write v2. Strict admission rejects unknown schemas, coercive booleans, malformed fields/lists, invalid enums/timestamps, accessors, hostile proxies, dangerous prototype keys, cycles, unsupported JSON values, and excessive argument depth/nodes/payload without throwing. Every supplied runtime definition is validated before planning or tool execution, including with no manifests, and invalid selected records remain visible as `workflow-invalid`. Text bounds, nested secret redaction, arbitrary-execution blocking, tool-identity review, acceptance injection, permission ranking, Agent-named IDs/tags/output markers, and valid v1 behavior remain compatible. The service implementation, former Agent-named Tasks/bootstrap paths and exports, coercive sanitizer, ambiguous export API, schema-only casts, and presentation-local definition decoder are deleted and restoration-gated; the private v1 decoder remains until durable forward migration is proven.
- The seven built-in Android workflow definitions, fixed IDs/order, default arguments, permission ceilings, sanitization, timestamps, redaction, and catalog membership are Tasks-owned through a dependency-injected catalog. Bootstrap supplies the shared workflow-definition policy and APK admission; explicit workflow selection, settings, and capability reporting consume target bindings. Ordinary Chat text cannot select these workflows locally. `agentAndroidWorkflows.ts` and its barrel export are deleted and gated.
- Workflow run-limit defaults, direct normalization, and settings projection are Tasks-owned through `workflowRunLimitPolicy`. `WorkflowRunLimits`, `WorkflowRunLimitSettings`, `DEFAULT_WORKFLOW_RUN_LIMITS`, `resolveWorkflowRunLimits`, and `resolveWorkflowRunLimitsFromSettings` are the only public names; Tasks policies, Chat bootstrap, MCP/provider/model-operation turns, and presentation confirmation consume them through `@/modules/tasks`. Exact defaults, bounds, truncation, immutable inputs, permission semantics, trace metadata, and fixed trace/background-continuation safeguards remain covered. The former `agentRunLimitPolicy` path, its Agent-named symbols, service definitions/exports, and stale compatibility-test mock are deleted and restoration-gated. The six persisted `agentWorkflow*` settings keys, historical `agent-run-*` identities, and visible Agent copy remain compatibility inputs pending versioned data/copy migration; new records use `workflow-run-*`.
- Workflow tool-permission decisions, evidence/limit evaluation, compatibility trace projection, and input-schema validation are Tasks-owned through `workflowToolPermissionPolicy` and the `workflowToolPolicy` bootstrap binding. `WorkflowToolPermission*`, `createWorkflowToolPermissionPolicy`, `decideWorkflowToolPermission`, and `validateWorkflowToolInput` are the active Tasks symbols; bootstrap supplies Integrations `decideToolPermission`, `resolveToolPermissionEvidence`, `resolveManifestExecutionPolicy`, schema validation, clock, and the existing trace sanitizer with no Workspaces authorization dependency. Omitted, Chat, Agent, and Companion compatibility inputs produce the same decision for the same tool, evidence, limits, and confirmation. The former `agentToolPermissionPolicy.ts` and `bootstrap/agentToolPolicy.ts` paths, `agentPolicy.ts` service facade, its service-barrel export, Workspaces mode-policy helpers, and the three unused permission/schema compatibility types are deleted and restoration-gated. Historical `AgentToolPermission` values, `agent-policy-*` trace IDs, `Agent policy ...` titles, persisted mode/evidence strings, and visible copy remain trace/data compatibility inputs until their durable schema migration is proven.
- Workflow orchestration is Tasks-owned through `createWorkflowOrchestrator`; `bootstrap/workflowOrchestrator` binds checkpoints, planning, continuation, step execution, observations, RAG evidence, pending actions, permission evidence, failure and completion policies, Android runtime state, and task-bound tools, then exposes `runWorkflow` to the single Chat workflow-entry consumer. Focused workflow and restart-recovery coverage preserves deterministic hashing inputs, cancellation, checkpoint ordering and signal identity, step-limit pauses, terminal persistence failure, Android state carry, RAG pause, terminal projection, and nonmutation. New runs are forward-written with `workflow-run-*`; historical `agent-run-*` values are not rewritten. The former Agent-named Tasks path, bootstrap path, five public contracts, unused input alias, and `runAgenticWorkflow` seam are deleted and restoration-gated. Legacy v1 workflow-definition data, checkpoint upgrade, persisted identity, and visible-copy compatibility remain separate migration inputs.
- Android capability validation now reads the Tasks workflow catalog, Integrations tool catalog, actual Android manifests/native bridge, operation audits, permission policy, and all seven evidence mappings. The duplicate `androidCapabilityBoundary.ts` definition is deleted; release evidence freshness follows the canonical workflow, tool, permission, capability-audit, and collector sources.
- Agent security evaluation now exercises the canonical prompt-injection, escalation, schema, permission, replay-identity, workflow-tampering, citation-repair, refusal, and fallback policies directly. The duplicate `agentSecurityEvaluation.ts` implementation and duplicated evaluator assertions are deleted, and the final `src/services/agent/index.ts` barrel is forbidden from returning.
- Chat workflow reply-to-message projection is Conversations-owned through a structural Chat-neutral contract. Status/error mapping, trace settling and classification, deterministic usage estimation, and cancellation projection are covered. Pending-action, evidence-repair, recovery, and continuation selection remains Tasks-owned compatibility behavior with the existing redaction and work-artifact contract bound in presentation; workflow-skill suggestion selection is presentation-owned with bounded/redacted canonical workflow reconstruction and fail-closed identity/execution-risk checks. The Agent-named Conversations projection and all five obsolete service selector definitions plus their sanitizer residue are deleted and restoration-gated.
- Chat workflow assistant-message resolution is Conversations-owned through a structural dependency-injected resolver. The Chat-named Tasks entry policy supplies the workflow runner while preserving one invocation, exact input/cancellation-signal and reply identity, handled/unhandled patch behavior, `startedAt` timing, failure propagation, and nonmutation. The Agent-named Conversations resolver, bootstrap binding, `agentMessageAdapter.ts`, and its service-barrel export are deleted and restoration-gated.
- Generic authority-tagged context contribution assembly is assistant-runtime-owned and bootstrap-composed across retrieval, web, MCP, and isolated Tavern lanes. `contextRuntime.ts` is deleted, and the conversation RAG runtime now propagates the exact task cancellation signal into knowledge retrieval.
- Tavern lifecycle, scope and active-link persistence, scoped export/import, current portable workspace replacement, Chat writeback receipts, and review/private-memory controls remain Workspaces-owned. Native uses current SQLite; browser uses the strict current-v2 key-value envelope under Web Locks. No Tavern v1 decoder, old storage selector, key-value-to-SQLite migration, or old multi-key merge remains.
- Tavern shaping-review reason classification, complete and safe review counts, and unresolved-scene detection are workspace-owned pure policy. The service classifier and duplicate presentation count/predicate implementations are deleted.
- Conversation-workspace context packing and narrative turn planning remain Workspaces-owned pure policy under historical Tavern-named data contracts. Bounded ranking, scene/character selection, hidden-memory privacy isolation, prompt/evidence formatting, speaker/narrator order, continuity, multilingual character stability, the exact user-input limit, and nonmutation remain covered. The generic conversation-workspace source consumes those policies internally and Chat receives only its public frozen source result; both legacy service definitions/exports are deleted.
- The consumer-free manual Tavern Chat/Agent payload formatter, its Workspaces public export, and its five `TavernHandoff*` data definitions are deleted and restoration-gated after source, persistence, portable-data, and presentation consumer audits. Run-bound Chat workspace context handoff and the versioned writeback handoff schema remain active and are not part of this removal.
- Tavern turn writeback proposal generation/application is workspace-owned pure policy. Multilingual summary/character/lore/relationship/scene parsing, stable IDs/evidence/order, ambiguity and stale-reference repair, privacy, immediate versus pending application, exact counts, timestamps, and nonmutation remain covered. Chat imports both APIs through `@/modules/workspaces`; the service definitions, proposal-only helpers, and transitional re-exports are deleted and gated.
- Tavern per-character stability diagnostics and relationship-state reports are workspace-owned pure policy. The live context policy consumes the per-character diagnostic; stable multilingual anchor ordering, score rounding, relationship kind ordering, and nonmutation remain covered. The consumer-free aggregate character-stability report and contract plus duplicate service builders/helpers/type re-exports are deleted and restoration-gated.
- Tavern lorebook draft application plus single/bulk approve and dismiss transitions are workspace-owned pure policy. Immediate and reviewed commits share stale-ID repair through unique normalized titles, fail closed on ambiguity, preserve explicit enabled-state changes, and apply bulk drafts in source order with matching last-write semantics. Unrelated review units and evidence survive; the four facade exports plus duplicate target/build/pending-unit helpers are deleted.
- Tavern character draft application plus single/bulk approve and dismiss transitions are workspace-owned pure policy. Immediate and reviewed commits share ID-first/normalized-name target repair, fail closed on ambiguous duplicate names, preserve existing identity/avatar/omitted fields and creation time, and apply bulk drafts in source order with matching last-write semantics. Sibling review units/evidence and character-before-memory/scene ordering survive; the four facade exports plus duplicate target/build helpers are deleted.
- Tavern private relationship-memory purge is workspace-owned pure policy. It removes only hidden confirmed memories and hidden pending candidates plus matching evidence, preserves visible and sibling review units, deletes newly empty pending writebacks, uses the injected timestamp, and does not mutate input. Chat workspace review composes this behavior through the Workspaces runtime; the former Companion direct consumer and facade export are deleted.
- Chat workspace review and private-memory control are Workspaces-owned through bounded projection and exact-revision compare-and-swap ports. Bootstrap selects current SQLite on React Native or the strict browser v2 key-value port under Web Locks. Current restart, CAS, cancellation, drift, receipt-preservation, and portable recovery coverage remains; no older-writer selector blocks deletion.
- Tavern whole pending-writeback dismissal is workspace-owned pure policy. Selected dismissal removes only the requested pending proposal and preserves sibling order; bulk dismissal clears the queue without applying any proposal. Both paths preserve unrelated collections, nonmutation, normalization, and injected timestamp semantics. Chat workspace review composes the required transition; the former Companion direct consumer and both facade exports are deleted.
- Chat stop, retry, regenerate, and stale-stream recovery are presentation-owned. Stop commits buffered text and traces before terminal accounting; retry/regenerate retain the preceding user message for history trimming but always start and report failures through Chat; stale recovery is conversation-scoped, idempotent, and awaits final persistence. The four legacy controls and their facade are removed while one fail-closed presentation runtime binding remains.
- Chat message projection and post-user model-first reply selection are presentation-owned. The dependency-injected dispatch controller sends normalized typed text directly to Assistant Runtime without reading Settings or consulting Agent intent; only explicit structured workflow IDs or work-artifact output controls enter the Chat-owned structured workflow lane. Bootstrap composes it before recovery/readiness; presentation imports neither bootstrap nor `chatRunner`.
- The consumer-free legacy `chatRunner.sendMessage` projection entry is deleted. Non-local dispatch re-reads and validates the exact latest store conversation after projecting one user turn, fails closed on a missing or duplicate projection, and invokes the bound runtime once. Repeating the same startup binding is idempotent; replacing it or sending before initialization fails closed.
- Chat post-user dispatch and retry/regenerate reply startup share one presentation-owned runtime binding installed only by bootstrap before recovery/readiness. Same-method rebinding is idempotent, competing replacement and uninitialized calls fail closed with distinct stable errors, send retains fire-and-forget scheduling, and controls continue awaiting startup in stop/trim/start order. `chatRunner.sendMessageAfterUserProjection`, its decision-context residue, the duplicate control state, and the module-evaluation binder are deleted and gated.
- Structured workflow reply startup is Conversations-owned and current-only. Bootstrap binds Chat task/activity, checkpoints, Knowledge, workflow resolution, error projection, and ordinary Chat fallback. No historical Agent/Companion metadata is admitted or consulted.
- New execution is Chat-owned across Assistant Runtime, Tasks, Conversations, presentation, bootstrap, and `/chat/:id`. Conversation/message/runtime contracts carry no product-mode field, current persistence contains no Agent/Companion metadata, and JavaScript extras cannot recreate it. Creation and loading use current SQLite records plus `ACTIVE_CONVERSATION` only.
- New explicit workflow reply startup now binds `createVNextChatWorkflowRuntime`, forwards the exact request controller signal, assembles the immutable context snapshot, and executes `AssistantRuntime.executeActivity({ kind: 'chat' })`. The exact Chat run identity reaches workflow resolution, checkpoints, and durable tasks; cancellation, context failure, terminal checkpoint failure, ordinary fallback, and input nonmutation are focused-gated. The Task-owned `agentRunUseCase.ts`, production `startVNextAgentRun`, Agent-named workflow bootstrap, its start/recovery controller code, and the public Agent run discriminator are deleted and restoration-gated. Startup terminally reconciles current Chat plus privately canonicalized historical storage rows through one Chat projection and then recovers linked durable tasks.
- New explicit Chat workflows publish `chat-workflow`; task activity admits only `chat-turn | chat-workflow`. `agent-run` and `tavern-turn` are not accepted as current activity inputs.
- The consumer-free `chatRunner.isConversationStreaming` facade is deleted and gated; presentation controls consume the stream lifecycle directly.
- Chat final-message copying is presentation-owned through an injected controller and Expo-composed command. It preserves exact `responseText ?? content` precedence, writes original untrimmed text, skips blank selections, propagates clipboard rejection to the existing UI handler, and mutates no message state. The two `chatRunner` clipboard helpers and its Expo Clipboard import are deleted.
- Chat workflow-skill saving is presentation-owned through `conversationWorkflowSkillSaveController`, the generic `saveApprovedConversationWorkflowSkillRuntime` seam, `saveConversationWorkflowSkillFromMessage`, and Chat-neutral bubble helper/callback names. It re-reads the current assistant message, preserves exact approval metadata and localized saved/already-saved/unavailable/blocked results, refreshes successful saves, and retains visible rejection feedback. Bootstrap binds the current Task-owned persistence and runtime snapshot policies under `workflowSkills`; Conversations and presentation forward only `enabledWorkflowIds` and `blockedWorkflowStates`. Tasks publicly exposes only generic suggestion, preview, approval, persistence, formatting, lifecycle/state, policy, and runtime-block contracts, and the Chat-neutral selector is the only presentation selector. The formatter now lives at and is exported through `workflowSkillFormattingPolicy.ts`; `agentWorkflowSkillFormattingPolicy.ts` is deleted with no alias, and its file and barrel-export restoration are gated. The `chatRunner` export/result type plus the Agent-named presentation controller, selector, suggestion/save/preview/formatter/lifecycle/state/policy/runtime-block contracts and methods, Tasks/bootstrap files, runtime option fields, runtime field/seam, command, helper, confirmation helper, and callback names are deleted and restoration-gated. The stable `not_agent_workflow` reason, translation keys, underlying Agent execution-definition and checkpoint contracts, and persisted workflow markers remain compatibility inputs pending their durable workflow-schema migration.
- Generic skill records and conversation projection are Conversations-owned through `createConversationSkillPolicy`, `createConversationSkillRepository`, `ConversationSkillRecordPort`, and `createConversationSkillApplication`. Bootstrap composes strict `SKILLS` reads/writes, configured Provider generation ranges, portable sanitization, identifiers, and localization, then binds the exact application into `conversationSkillCommand` before hydration. Record mutations are serialized, preserve newest-first upsert and existing delete order, filter invalid entries using the v1 normalizer, and perform no write after a failed read. Settings, Composer, workflow policy, plugin catalog, and toolchain catalog no longer import `src/services/skills.ts`; that service is deleted and restoration-gated. The physical `SKILLS` key and `islemind.skill.v1` / `islemind.skill.portable.v2` schemas remain unchanged, so no data rewrite is required.
- Chat pending-action confirmation is presentation-owned through the generic controller and current message-action command. Catalog discovery is parameterless, current conversation identity is reread, and restart receives only the exact tool request, current limits, and `userConfirmed: true`. No previous-user product mode or compatibility drift check remains.
- Tavern key-value-to-SQLite migration is deleted. Workspaces no longer exports migration factories, source/target ports, marker/state storage, or `source_receipts_unmigrated`; bootstrap no longer invokes a key-value-to-SQLite or old multi-key merge. Current native SQLite and browser v2 key-value repositories are tested independently.
- Tavern snapshot export filtering, omission auditing, scope duplication, active-link filtering, and scoped export/import orchestration are workspace-owned. Public-only defaults, explicit hidden/pending inclusion, hidden evidence filtering, deterministic normalization, exact counts, nonmutation, atomic import, and collision-safe duplication remain covered. Portable storage obtains contracts through `@/modules/workspaces` and lifecycle operations through bootstrap; all facade definitions, helpers, and the final facade are deleted and gated.
- Tavern narrative-summary single/bulk approval and dismissal are workspace-owned pure policy. Exact current scene IDs survive while stale IDs clear, only matching summary evidence is removed, sibling review units and composite ordering remain intact, and injected timestamps/nonmutation are covered. The four facade exports are deleted.
- The consumer-free Tavern narrative turn planner is deleted from Workspaces together with `TAVERN_NARRATIVE_TURN_SCHEMA`, its speaker/plan/options contracts, and private queue/directive helpers. `tavern-core` and architecture restoration gates reject their return. Narrative summaries, context packing, snapshots, review/writeback, persistence, migrations, and portable recovery remain intact, so this deletion changes no stored schema, key, decoder, or historical record.
- Tavern scene-change single/bulk approval and dismissal plus proposal application/reference repair are workspace-owned pure policy. Existing, new, branched, stale, and readable-repair cases retain legacy behavior; approval preserves chronological bulk traversal, dismissal preserves normalized queue traversal, exact evidence and sibling review units survive correctly, and injected timestamps/nonmutation are covered. The four review facade exports and duplicate service helpers are deleted; legacy writeback application still calls the target proposal-application policy.
- Tavern relationship-memory single, per-writeback, new-only, and global review transitions plus candidate character-reference repair are workspace-owned pure policy. New/duplicate/conflict semantics, replace behavior, stale/readable repair, ambiguity failure, exact evidence cleanup, sibling units, queue traversal, injected timestamps, and nonmutation remain covered. The former Companion direct imports are deleted with the presentation island; the nine review facade definitions/exports and duplicate repair helper remain deleted. Workspaces composite/shaping/whole-writeback orchestration composes the target transitions, while legacy immediate writeback application still uses target reference repair.
- Tavern shaping-suggestion and whole pending-writeback single/global approval orchestration are workspace-owned pure policy. Shaping approval preserves character-memory-lore-scene order and chronological queue traversal; whole-writeback approval preserves per-writeback summary-character-lore-new-memory-scene order and global phase ordering. Exact evidence cleanup, stale reference repair, duplicate/conflict candidates, sibling units, injected timestamps, and nonmutation remain covered; the four facade definitions/exports and approval-only imports are deleted.
- Tavern single/bulk shaping-suggestion dismissal is workspace-owned pure policy. It removes only reason-matched character, lore, memory, and scene proposals plus exact evidence, preserves non-shaping siblings and queue semantics, deletes only newly empty writebacks, and retains injected timestamps/nonmutation. The two dismissal facade exports are deleted.
- Tavern contracts plus the strict canonical snapshot codec/clone, lenient legacy normalization, bounded ordering/dedup/default policies, six upserts, and deletion are workspace-owned. Direct target consumers use `@/modules/workspaces`, `src/services/tavern/contracts.ts` is deleted and gated, and backup-first schemaless migration remains lenient before strict target verification.
- Character Card v2 and WorldInfo serialization/admission are workspace-owned. Direct Studio consumers use `@/modules/workspaces`; the four service APIs and interchange-only helpers are removed. Imports remain intentionally lenient, collections are bounded, WorldInfo identity/order/disabled state and Character avatar metadata survive Studio projection, and multi-entry books fail closed at the single-entry editor boundary.
- MCP execution admission and built-in/remote dispatch are target-owned; `mcpExecutionAdapter.ts` is deleted while bootstrap preserves localized results, logging, and cancellation timing.
- MCP and built-in dispatch preserve the exact durable task ID when one exists across workflow execution, bootstrap `callMcpTool`, and the Integrations policy; focused coverage also protects the synthesized ID fallback for non-task-bound compatibility callers. Ordinary Chat tagged fallback now preserves the provider-admitted canonical source and identity, including `builtin:islemind-builtins:search_web`, and considers provider manifests only for a bounded parsed tagged request.
- Built-in capability contracts, immutable permission/confirmation policy, deterministic runnable-port discovery, and adapters are Integrations-owned and exported through `@/modules/integrations`; `createBuiltInCapabilityRuntimeBinding` is bootstrap-composed without deep imports. Bootstrap independently verifies the exact canonical permission scope against a running, allowed durable task before attesting. `search_web`, Android native-trusted Fetch with Tavily fallback `crawl_web`, Knowledge-backed read-only `read_file`, and native SQLite-backed `edit_file` are production-bound with bounded output, cancellation, and fail-closed validation. Android crawl target admission resolves every address, rejects non-public results, issues a short-lived single-use permit, disables proxies and automatic redirects, and pins OkHttp DNS for the actual connection. The target chooses Tavily only when the complete local trust/fetch pair is absent and never after a local trust or fetch failure. The read router keeps `knowledge/*` read-only and routes `workspace/*` through the writable port only when native durable SQLite is available; web therefore never advertises file editing. The two unimplemented media capability contracts are deleted and restoration-gated. The legacy registry path is deleted and restoration is gated.
- MCP catalog policy plus reusable HTTP client/session discovery and execution are integrations-owned and bootstrap-composed through one shared singleton; `mcpCatalogAdapter.ts` and `mcpClientAdapter.ts` are deleted. Cancelled discovery does not persist an error state or invalidate sessions, post-commit cancellation reconciles committed state before surfacing cancellation, ordinary discovery failure persists and logs the error state, endpoint/transport changes clear reusable sessions, and queued cancellation cannot start a remote request after an await boundary.
- Android device execution now creates the target external observation at its source boundary; the local Agent trace-shaped result and Android-specific flat-result normalization are deleted while operation audit metadata and cancellation remain intact.
- The consumer-free toolchain runtime-event sink/orchestration stack and all 15 `toolchain.*` event kinds are deleted. Lifecycle event-data composition retains only the live registered-launch projection; test-only task, pairing, MCP-session, and cancellation projections are removed.
- Consumer-free runtime-pairing, handoff, registered-execution, runtime-report application, MCP/CLI report projection, runtime-report identity, and CLI execution-application code plus their test-only compatibility blocks are deleted. The validated CLI catalog remains, but no CLI production execution path is claimed.
- Consumer-free MCP gateway-session construction and runtime-report envelope/gateway admission are deleted. Live endpoint sanitization and persisted task log/artifact trust remain target-owned.
- Execution-event admission now retains only the production runtime-handoff trust decision. Lifecycle composition binds it directly; the transitional execution-event service facade, consumer-free task-request, CLI-plan, and registered-plan admission methods, and compatibility-only runtime-report dependency fields are removed.
- Toolchain task request, confirmation, record, bounded log/artifact, transition, and expiry behavior is integrations-owned through one dependency-injected policy. Service defaults provide schemas, limits, permissions, and trusted-runtime admission; the `taskRuntime.ts` implementation is deleted while strict input admission, CLI catalog checks, confirmation matching, redaction, deterministic IDs, and cancellation-compatible transitions remain covered.
- Runtime schemas, kinds, capabilities, pairing policy, and snapshot trust are integrations-owned through `TOOLCHAIN_RUNTIME_PAIRING_POLICY` and `TOOLCHAIN_RUNTIME_SNAPSHOT_POLICY`; `runtimePairingBinding.ts` and `runtimeSnapshotBinding.ts` are deleted, and remaining service/bootstrap composition consumes the target public policies.
- Toolchain schemas, public runtime contracts, guards, deterministic identity/IDs, and bounded primitives are integrations-owned and consumed through `@/modules/integrations`; the six service support shims are deleted and the architecture gate forbids their return.
- Toolchain configured defaults and live lifecycle composition are bootstrap-owned through `toolchainComposition.ts` and `toolchainLifecycleEventPolicy.ts`. The final two `src/services/toolchain/**` files are deleted and gated; no service toolchain directory remains.
- Superseded architecture notes, compatibility documents, duplicate provider-import scripts, and obsolete completion gates are deleted under the least-necessary policy.
- The vNext architecture boundary gate requires the plan, public API manifest, and migration status, and rejects every additional file under `docs/architecture`; retired evidence or compatibility notes cannot be restored without failing the existing architecture gate.

## Validation

Focused validation for the current integrated slices:

- `bun run type-check`
- `bun run test:credential-governance-compatibility`
- `bun run test:provider-intelligence -- --focus=portable-import`
- `bun run test:runtime-privacy-retention-compatibility`
- `bun scripts/agent-tool-policy-tests.js`
- `bun run test:provider-intelligence --focus=generation-parameters`
- `bun run test:provider-parameter-matrix`
- `node scripts/provider-intelligence-tests.js --focus=assistant-conversation-request-planning`
- `node scripts/provider-intelligence-tests.js --focus=context-planner`
- `node scripts/provider-intelligence-tests.js --focus=upstream-governance`
- `bun run test:provider-intelligence --focus=web-search-settings-policy`
- `node scripts/provider-intelligence-tests.js --focus=built-in-search-runtime`
- `bun run test:provider-intelligence --focus=built-in-remote-crawl-runtime`
- `bun run test:provider-intelligence -- --focus=assistant-conversation-provider-tool-admission`
- `node scripts/provider-intelligence-tests.js --focus=remote-compact-lifecycle`
- `node scripts/provider-intelligence-tests.js`
- `bun run test:provider-intelligence --focus=assistant-conversation-finalization`
- `bun run test:provider-intelligence -- --focus=assistant-conversation-durable-execution`
- `bun run test:vnext-conversation-message-command`
- `bun run test:chat-session`
- `bun run test:provider-intelligence -- --focus=provider-store-cleanup`
- `bun run test:chat-error-isolation`
- `bun run test:vnext-presentation-controller`
- `bun run test:streaming-cleanup`
- `bun run test:vnext-agent-run`
- `bun run test:vnext-agent-task-adapter`
- `bun run test:vnext-task-runtime`
- `bun run test:chat-task-runtime`
- `bun run test:vnext-architecture-contract`
- `bun run test:agent-workflow`
- `bun run test:agent-workflow-compatibility`
- `bun run test:tool-calling-compatibility`
- `bun run test:memory-governance`
- `bun run test:structured-output-compatibility`
- `bun run test:context-engineering-compatibility`
- `bun run test:android-workflow-templates`
- `node scripts/provider-intelligence-tests.js --focus=assistant-conversation-reply-start`
- `node scripts/provider-intelligence-tests.js --focus=assistant-conversation-stream-lifecycle`
- `node scripts/provider-intelligence-tests.js`
- `bun run test:provider-runtime-health-compatibility`
- `node scripts/provider-state-isolation-compatibility-tests.js`
- `bun run test:local-inference-compatibility`
- `bun run test:context-compression-v2`
- `node scripts/agent-rag-quality-tests.js`
- `bun run test:tavern-core`
- `node scripts/provider-intelligence-tests.js --focus=portable-import`
- `bun run test:tavern-runtime-context`
- `bun run test:tavern-ui`
- `bun run test:vnext-walking-skeleton`
- `bun run test:mcp-compatibility`
- `bun run test:toolchain-catalog`
- `node scripts/toolchain-runtime-compatibility-tests.js`
- `node scripts/architecture-boundary-audit.js --self-test`
- `bun run test:architecture-boundary`
- `bun run test:architecture-dependencies`
- `bun run test:theme-system`
- `bun run test:product-mobile-layout`
- `bun run test:product-experience-compatibility`
- `bun run test:product-mobile-web-render-evidence`
- `bun run test:product-mobile-native-evidence`
- `bun run test:provider-intelligence --focus=provider-client-simulation`
- `bun scripts/collect-mock-provider-chat-android.js --self-test`
- `bun scripts/collect-provider-runtime-android.js --self-test`
- `node scripts/provider-intelligence-tests.js --focus=provider-runtime-android`
- `bun scripts/collect-long-content-request-log.js --self-test`
- `bun scripts/raw-evidence-contracts.js`
- `bun scripts/raw-evidence-contracts.js --self-test`
- `bun scripts/qa-coverage-audit.js --self-test`
- `node --check scripts/build-local-android-apk.js`
- `ISLEMIND_ANDROID_JAVA_HOME=G:\Project\IsleMind\missing-jdk-17 node scripts/build-local-android-apk.js --release --variant no-model --release-arch x86_64` (expected fail-closed before prebuild/Gradle)
- source-level invocation of `selectAndroidJavaHome()` / `androidBuildEnv()` from `scripts/build-local-android-apk.js` proving Java 17 selection at `G:\dev\managers\mise\data\installs\java\17.0.2` and Windows `Path` preservation
- `node --check scripts/collect-settings-state-android.js`
- `node scripts/collect-settings-state-android.js --self-test`
- `QA_DEVICE_SERIAL=dadaa813 QA_ALLOW_DESTRUCTIVE_SETTINGS_ROUNDTRIP=1 node scripts/collect-settings-state-android.js --portable-roundtrip-only`
- `bun run apk:local:release -- --release-arch x86_64`
- `QA_DEVICE_SERIAL=emulator-5554 bun run release:install-current-apk -- --device emulator-5554`
- `QA_DEVICE_SERIAL=emulator-5554 bun run test:current-apk-smoke` (passed in the current aggregate QA audit; source freshness, APK/device SHA-256 parity, clean-install identity, launch PID stability, fatal-log absence, and 16 KB validation are current)

The strict application-record fixture covers typed read/write/remove failure, diagnostics failure, retained legacy null/no-throw behavior, portable-export rejection, and no-write-after-read-failure behavior for MCP and the toolchain registered catalog. The focused vNext architecture, MCP, toolchain catalog/runtime, portable-import, privacy, credential, Tavern, boundary/self-test, dependency, and type gates passed serially on 2026-07-28. Current release-device compact and 12 MiB portable round trips pass on `dadaa813`. Earlier bounded desktop and Android Chromium CDP paths timed out, but the current browser-native Microsoft Edge page transport now proves persistent-profile restart and two-real-tab cross-context Web Locks for the production AsyncStorage Tavern adapter.

The 2026-07-30 final source checkpoint for the workflow-settings presentation cutover passes `test:product-experience-compatibility`, `test:agent-workflow-compatibility`, strict type-check, dependency audit with zero cycles, the vNext walking skeleton, the vNext architecture contract, the architecture-boundary self-test, and the combined architecture boundary gate at 14/14 with zero blocking issues; the vNext boundary covers 536 files with zero issues. The audit now requires canonical `focus: 'workflow'` emission, neutral Settings focus types/helpers, normalization of the one historical `focus=agent-workflow` route input, and Workflow-neutral visible locale values. Its self-test and forbidden markers reject restoration of the Agent-named emitter, route output, type, helpers, and list/definition symbols.

The subsequent 2026-07-30 bounded source checkpoint deletes the consumer-free Tavern narrative planner/contracts and neutralizes the Chat workflow-resolution dependency/helper/reason names. `test:tavern-core`, `test:agent-workflow-compatibility`, strict type-check, the zero-cycle dependency audit, the vNext walking skeleton, the vNext architecture contract, the architecture-boundary mutation self-test, and the combined boundary gate at 14/14 with zero findings all pass. Restoration gates cover both deleted surfaces, and the checkpoint changes no persistence or historical compatibility contract.

The current `toolchain-runtime-compatibility-tests.js` fixture covers all four external-agent product descriptors, matching successful request admission for Codex CLI, Codex Desktop, Claude Code, and Grok CLI, three protocol mappings, frozen public discriminator and product-to-task-capability sets, unchanged request/outcome v1 wire keys, exact-key descriptor/request/outcome admission, immutable detached copies without mutating or freezing caller-owned descriptor/request input, exact product-matched trusted desktop task authority, unrelated and cross-product task rejection, zero port work after execution-time capability mismatch, Android-hosted Termux rejection, before/at/after task-expiry admission plus no-dispatch after expiry, runtime/task/request drift, caller-supplied resume correlation, stale and malformed outcomes, secret/path/command field rejection, and pre/post-dispatch cancellation without repeat port work. The focused `bun run test:toolchain-runtime-compatibility` gate passed on 2026-07-28. This is source/contract evidence only: no vendor binary, concrete adapter, bootstrap composition, persistence, signal-ownership binding, permission/sandbox adapter, mobile companion, desktop process, native build, or end-to-end runtime session was exercised.

The latest focused coordinator validation covers strict Chat AssistantRun admission, mode-free conversation persistence, current SQLite conversations plus `ACTIVE_CONVERSATION`, current Workspaces SQLite/browser-v2 paths, portable recovery without the AsyncStorage blob fallback, restoration gates, and the supported serial architecture tests. Non-Chat rows and removed storage envelopes fail closed. No validation result is used to claim support for retired writers or formats.
